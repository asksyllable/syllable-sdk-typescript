name: Publish Release Candidate
permissions:
  checks: write
  contents: write
  pull-requests: write
  statuses: write
"on":
  workflow_run:
    workflows: ["Auto-merge Speakeasy Bot PR to generate SDK"]
    types:
      - completed
  workflow_dispatch: {}
jobs:
  check-rc-version:
    runs-on: ubuntu-latest
    outputs:
      is_rc: ${{ steps.check.outputs.is_rc }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      
      - name: Debug push info
        run: |
          echo "=== Push Debug Info ==="
          echo "Push Actor: ${{ github.actor }}"
          echo "Push Ref: ${{ github.ref }}"
          echo "Push Head Commit: ${{ github.sha }}"
          echo "======================"
      
      - name: Check if version is RC
        id: check
        run: |
          # Check current version in gen.yaml
          VERSION=$(grep -E '^\s*version:\s*' .speakeasy/gen.yaml | sed 's/.*version:\s*//' | tr -d ' ')
          echo "Current version in gen.yaml: $VERSION"
          
          # Check if version contains RC
          if [[ $VERSION == *"-rc"* ]]; then
            echo "is_rc=true" >> $GITHUB_OUTPUT
            echo "✅ RC version detected: $VERSION - will publish"
          else
            echo "is_rc=false" >> $GITHUB_OUTPUT
            echo "❌ Stable version detected: $VERSION - skipping RC publish"
          fi

  # publish:
  #   needs: check-rc-version
  #   if: needs.check-rc-version.outputs.is_rc == 'true'
  #   uses: speakeasy-api/sdk-generation-action/.github/workflows/sdk-publish.yaml@v15
  #   with:
  #     target: syllable-sdk
  #   secrets:
  #     github_access_token: ${{ secrets.GITHUB_TOKEN }}
  #     npm_token: ${{ secrets.NPM_TOKEN }}
  #     speakeasy_api_key: ${{ secrets.SPEAKEASY_API_KEY }}
