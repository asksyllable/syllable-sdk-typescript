/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ExperimentsStopRequest = {
  experimentId: number;
};

/** @internal */
export const ExperimentsStopRequest$inboundSchema: z.ZodType<
  ExperimentsStopRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  experiment_id: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "experiment_id": "experimentId",
  });
});
/** @internal */
export type ExperimentsStopRequest$Outbound = {
  experiment_id: number;
};

/** @internal */
export const ExperimentsStopRequest$outboundSchema: z.ZodType<
  ExperimentsStopRequest$Outbound,
  z.ZodTypeDef,
  ExperimentsStopRequest
> = z.object({
  experimentId: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    experimentId: "experiment_id",
  });
});

export function experimentsStopRequestToJSON(
  experimentsStopRequest: ExperimentsStopRequest,
): string {
  return JSON.stringify(
    ExperimentsStopRequest$outboundSchema.parse(experimentsStopRequest),
  );
}
export function experimentsStopRequestFromJSON(
  jsonString: string,
): SafeParseResult<ExperimentsStopRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExperimentsStopRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExperimentsStopRequest' from JSON`,
  );
}
