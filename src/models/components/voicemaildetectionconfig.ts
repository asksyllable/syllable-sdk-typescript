/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export const Mode = {
  V1: "v1",
  V2: "v2",
} as const;
export type Mode = ClosedEnum<typeof Mode>;

/**
 * Voicemail detection config stored on outbound_campaign.voicemail_detection.
 *
 * @remarks
 *
 * `mode` selects the (mutually-exclusive) strategy; the remaining fields are detection timeouts.
 * Unknown keys are preserved (`extra='allow'`) so the schema stays backwards compatible with blobs
 * written before fields were added. See ZOO-7818.
 */
export type VoicemailDetectionConfig = {
  /**
   * Voicemail-detection strategy: 'v1' (original check_voicemail/leave_voicemail) or 'v2' (Voicemail Detection 2, which suppresses the turn-0 greeting and uses the greeting tool). The two modes are mutually exclusive. Omitted/null on legacy v1 campaigns is treated as v1.
   */
  mode?: Mode | null | undefined;
  voicemailDetectionOverallTimeout?: number | null | undefined;
  voicemailDetectionPreSpeechTimeout?: number | null | undefined;
  voicemailDetectionPostSpeechTimeout?: number | null | undefined;
  voicemailDetectionSpeechThreshold?: number | null | undefined;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const Mode$inboundSchema: z.ZodNativeEnum<typeof Mode> = z.nativeEnum(
  Mode,
);
/** @internal */
export const Mode$outboundSchema: z.ZodNativeEnum<typeof Mode> =
  Mode$inboundSchema;

/** @internal */
export const VoicemailDetectionConfig$inboundSchema: z.ZodType<
  VoicemailDetectionConfig,
  z.ZodTypeDef,
  unknown
> = collectExtraKeys$(
  z.object({
    mode: z.nullable(Mode$inboundSchema).optional(),
    voicemail_detection_overall_timeout: z.nullable(z.number()).optional(),
    voicemail_detection_pre_speech_timeout: z.nullable(z.number()).optional(),
    voicemail_detection_post_speech_timeout: z.nullable(z.number()).optional(),
    voicemail_detection_speech_threshold: z.nullable(z.number()).optional(),
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "voicemail_detection_overall_timeout": "voicemailDetectionOverallTimeout",
    "voicemail_detection_pre_speech_timeout":
      "voicemailDetectionPreSpeechTimeout",
    "voicemail_detection_post_speech_timeout":
      "voicemailDetectionPostSpeechTimeout",
    "voicemail_detection_speech_threshold": "voicemailDetectionSpeechThreshold",
  });
});
/** @internal */
export type VoicemailDetectionConfig$Outbound = {
  mode?: string | null | undefined;
  voicemail_detection_overall_timeout?: number | null | undefined;
  voicemail_detection_pre_speech_timeout?: number | null | undefined;
  voicemail_detection_post_speech_timeout?: number | null | undefined;
  voicemail_detection_speech_threshold?: number | null | undefined;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const VoicemailDetectionConfig$outboundSchema: z.ZodType<
  VoicemailDetectionConfig$Outbound,
  z.ZodTypeDef,
  VoicemailDetectionConfig
> = z.object({
  mode: z.nullable(Mode$outboundSchema).optional(),
  voicemailDetectionOverallTimeout: z.nullable(z.number()).optional(),
  voicemailDetectionPreSpeechTimeout: z.nullable(z.number()).optional(),
  voicemailDetectionPostSpeechTimeout: z.nullable(z.number()).optional(),
  voicemailDetectionSpeechThreshold: z.nullable(z.number()).optional(),
  additionalProperties: z.record(z.any()).optional(),
}).transform((v) => {
  return {
    ...v.additionalProperties,
    ...remap$(v, {
      voicemailDetectionOverallTimeout: "voicemail_detection_overall_timeout",
      voicemailDetectionPreSpeechTimeout:
        "voicemail_detection_pre_speech_timeout",
      voicemailDetectionPostSpeechTimeout:
        "voicemail_detection_post_speech_timeout",
      voicemailDetectionSpeechThreshold: "voicemail_detection_speech_threshold",
      additionalProperties: null,
    }),
  };
});

export function voicemailDetectionConfigToJSON(
  voicemailDetectionConfig: VoicemailDetectionConfig,
): string {
  return JSON.stringify(
    VoicemailDetectionConfig$outboundSchema.parse(voicemailDetectionConfig),
  );
}
export function voicemailDetectionConfigFromJSON(
  jsonString: string,
): SafeParseResult<VoicemailDetectionConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VoicemailDetectionConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VoicemailDetectionConfig' from JSON`,
  );
}
