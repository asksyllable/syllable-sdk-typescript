/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Twilio-side A2P setup state for the number.
 *
 * @remarks
 *
 * Reflects Twilio configuration (Messaging Service + Brand + Campaign records); it is not
 * carrier per-number REGISTERED state or legal/content compliance.
 */
export type A2pMessagingPathCheckResponse = {
  /**
   * True when the number is on a Messaging Service with a US A2P registration whose brand is approved and campaign is verified on the same registration.
   */
  a2pApproved: boolean;
};

/** @internal */
export const A2pMessagingPathCheckResponse$inboundSchema: z.ZodType<
  A2pMessagingPathCheckResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  a2p_approved: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "a2p_approved": "a2pApproved",
  });
});
/** @internal */
export type A2pMessagingPathCheckResponse$Outbound = {
  a2p_approved: boolean;
};

/** @internal */
export const A2pMessagingPathCheckResponse$outboundSchema: z.ZodType<
  A2pMessagingPathCheckResponse$Outbound,
  z.ZodTypeDef,
  A2pMessagingPathCheckResponse
> = z.object({
  a2pApproved: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    a2pApproved: "a2p_approved",
  });
});

export function a2pMessagingPathCheckResponseToJSON(
  a2pMessagingPathCheckResponse: A2pMessagingPathCheckResponse,
): string {
  return JSON.stringify(
    A2pMessagingPathCheckResponse$outboundSchema.parse(
      a2pMessagingPathCheckResponse,
    ),
  );
}
export function a2pMessagingPathCheckResponseFromJSON(
  jsonString: string,
): SafeParseResult<A2pMessagingPathCheckResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => A2pMessagingPathCheckResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'A2pMessagingPathCheckResponse' from JSON`,
  );
}
