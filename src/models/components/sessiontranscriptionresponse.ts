/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SessionAction,
  SessionAction$inboundSchema,
  SessionAction$Outbound,
  SessionAction$outboundSchema,
} from "./sessionaction.js";
import {
  SessionText,
  SessionText$inboundSchema,
  SessionText$Outbound,
  SessionText$outboundSchema,
} from "./sessiontext.js";

/**
 * Text transcript of a given session.
 */
export type SessionTranscriptionResponse = {
  /**
   * Internal ID of the session
   */
  sessionId: string;
  /**
   * Transcriptions of all messages in the session
   */
  transcription: Array<SessionText>;
  /**
   * Tool invocations that occurred during the session
   */
  actions: Array<SessionAction>;
};

/** @internal */
export const SessionTranscriptionResponse$inboundSchema: z.ZodType<
  SessionTranscriptionResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  session_id: z.string(),
  transcription: z.array(SessionText$inboundSchema),
  actions: z.array(SessionAction$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "session_id": "sessionId",
  });
});

/** @internal */
export type SessionTranscriptionResponse$Outbound = {
  session_id: string;
  transcription: Array<SessionText$Outbound>;
  actions: Array<SessionAction$Outbound>;
};

/** @internal */
export const SessionTranscriptionResponse$outboundSchema: z.ZodType<
  SessionTranscriptionResponse$Outbound,
  z.ZodTypeDef,
  SessionTranscriptionResponse
> = z.object({
  sessionId: z.string(),
  transcription: z.array(SessionText$outboundSchema),
  actions: z.array(SessionAction$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    sessionId: "session_id",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace SessionTranscriptionResponse$ {
  /** @deprecated use `SessionTranscriptionResponse$inboundSchema` instead. */
  export const inboundSchema = SessionTranscriptionResponse$inboundSchema;
  /** @deprecated use `SessionTranscriptionResponse$outboundSchema` instead. */
  export const outboundSchema = SessionTranscriptionResponse$outboundSchema;
  /** @deprecated use `SessionTranscriptionResponse$Outbound` instead. */
  export type Outbound = SessionTranscriptionResponse$Outbound;
}

export function sessionTranscriptionResponseToJSON(
  sessionTranscriptionResponse: SessionTranscriptionResponse,
): string {
  return JSON.stringify(
    SessionTranscriptionResponse$outboundSchema.parse(
      sessionTranscriptionResponse,
    ),
  );
}

export function sessionTranscriptionResponseFromJSON(
  jsonString: string,
): SafeParseResult<SessionTranscriptionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionTranscriptionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionTranscriptionResponse' from JSON`,
  );
}
