/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Per-request_status breakdown with total count and optional channel_manager_status counts.
 */
export type RequestStatusBreakdown = {
  /**
   * Total number of requests with this request_status
   */
  totalCount: number;
  /**
   * Counts by channel_manager_status within this request_status; omitted if none
   */
  counts?: { [k: string]: number } | null | undefined;
};

/** @internal */
export const RequestStatusBreakdown$inboundSchema: z.ZodType<
  RequestStatusBreakdown,
  z.ZodTypeDef,
  unknown
> = z.object({
  total_count: z.number().int(),
  counts: z.nullable(z.record(z.number().int())).optional(),
}).transform((v) => {
  return remap$(v, {
    "total_count": "totalCount",
  });
});
/** @internal */
export type RequestStatusBreakdown$Outbound = {
  total_count: number;
  counts?: { [k: string]: number } | null | undefined;
};

/** @internal */
export const RequestStatusBreakdown$outboundSchema: z.ZodType<
  RequestStatusBreakdown$Outbound,
  z.ZodTypeDef,
  RequestStatusBreakdown
> = z.object({
  totalCount: z.number().int(),
  counts: z.nullable(z.record(z.number().int())).optional(),
}).transform((v) => {
  return remap$(v, {
    totalCount: "total_count",
  });
});

export function requestStatusBreakdownToJSON(
  requestStatusBreakdown: RequestStatusBreakdown,
): string {
  return JSON.stringify(
    RequestStatusBreakdown$outboundSchema.parse(requestStatusBreakdown),
  );
}
export function requestStatusBreakdownFromJSON(
  jsonString: string,
): SafeParseResult<RequestStatusBreakdown, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RequestStatusBreakdown$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RequestStatusBreakdown' from JSON`,
  );
}
