/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Request model to restore directory member.
 */
export type DirectoryMemberRestore = {
  /**
   * Comment stored in version history for this restore
   */
  comments?: string | undefined;
};

/** @internal */
export const DirectoryMemberRestore$inboundSchema: z.ZodType<
  DirectoryMemberRestore,
  z.ZodTypeDef,
  unknown
> = z.object({
  comments: z.string().default("Restored"),
});
/** @internal */
export type DirectoryMemberRestore$Outbound = {
  comments: string;
};

/** @internal */
export const DirectoryMemberRestore$outboundSchema: z.ZodType<
  DirectoryMemberRestore$Outbound,
  z.ZodTypeDef,
  DirectoryMemberRestore
> = z.object({
  comments: z.string().default("Restored"),
});

export function directoryMemberRestoreToJSON(
  directoryMemberRestore: DirectoryMemberRestore,
): string {
  return JSON.stringify(
    DirectoryMemberRestore$outboundSchema.parse(directoryMemberRestore),
  );
}
export function directoryMemberRestoreFromJSON(
  jsonString: string,
): SafeParseResult<DirectoryMemberRestore, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DirectoryMemberRestore$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DirectoryMemberRestore' from JSON`,
  );
}
