/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BridgePhrasesConfigPayload,
  BridgePhrasesConfigPayload$inboundSchema,
  BridgePhrasesConfigPayload$Outbound,
  BridgePhrasesConfigPayload$outboundSchema,
} from "./bridgephrasesconfigpayload.js";

/**
 * Request model to create a bridge phrases config.
 */
export type BridgePhrasesCreateRequest = {
  /**
   * The name of the bridge phrases config.
   */
  name: string;
  /**
   * Description of the bridge phrases config.
   */
  description?: string | null | undefined;
  /**
   * The `config` JSONB payload stored on a bridge_phrases row.
   *
   * @remarks
   *
   * Holds the default phrases plus an inline list of per-tool overrides, each with
   * optional per-language (`localized`) overrides. Mirrors the shape documented in
   * docs/bridge-phrases-table-migration.md §5.2.
   */
  config?: BridgePhrasesConfigPayload | undefined;
  /**
   * Whether this config should be marked as the default for its suborg. At most one non-deleted config per suborg may be the default; the API returns a 400 if a second default is requested while another is already set.
   */
  isDefault?: boolean | undefined;
};

/** @internal */
export const BridgePhrasesCreateRequest$inboundSchema: z.ZodType<
  BridgePhrasesCreateRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string(),
  description: z.nullable(z.string()).optional(),
  config: BridgePhrasesConfigPayload$inboundSchema.optional(),
  is_default: z.boolean().default(false),
}).transform((v) => {
  return remap$(v, {
    "is_default": "isDefault",
  });
});
/** @internal */
export type BridgePhrasesCreateRequest$Outbound = {
  name: string;
  description?: string | null | undefined;
  config?: BridgePhrasesConfigPayload$Outbound | undefined;
  is_default: boolean;
};

/** @internal */
export const BridgePhrasesCreateRequest$outboundSchema: z.ZodType<
  BridgePhrasesCreateRequest$Outbound,
  z.ZodTypeDef,
  BridgePhrasesCreateRequest
> = z.object({
  name: z.string(),
  description: z.nullable(z.string()).optional(),
  config: BridgePhrasesConfigPayload$outboundSchema.optional(),
  isDefault: z.boolean().default(false),
}).transform((v) => {
  return remap$(v, {
    isDefault: "is_default",
  });
});

export function bridgePhrasesCreateRequestToJSON(
  bridgePhrasesCreateRequest: BridgePhrasesCreateRequest,
): string {
  return JSON.stringify(
    BridgePhrasesCreateRequest$outboundSchema.parse(bridgePhrasesCreateRequest),
  );
}
export function bridgePhrasesCreateRequestFromJSON(
  jsonString: string,
): SafeParseResult<BridgePhrasesCreateRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BridgePhrasesCreateRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BridgePhrasesCreateRequest' from JSON`,
  );
}
