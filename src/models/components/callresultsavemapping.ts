/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  CaseExpression,
  CaseExpression$inboundSchema,
  CaseExpression$Outbound,
  CaseExpression$outboundSchema,
} from "./caseexpression.js";
import {
  CelExpression,
  CelExpression$inboundSchema,
  CelExpression$Outbound,
  CelExpression$outboundSchema,
} from "./celexpression.js";
import {
  JMESPathExpression,
  JMESPathExpression$inboundSchema,
  JMESPathExpression$Outbound,
  JMESPathExpression$outboundSchema,
} from "./jmespathexpression.js";

export type From1 =
  | CelExpression
  | (JMESPathExpression & { type: "jmespath" })
  | (JMESPathExpression & { type: "jp" });

/**
 * Expression evaluated against the parsed tool response body (the JSON the external tool returned). Supported forms: (1) JMESPath string (default for plain strings) — a bare field name for raw passthrough, or a boolean/computed expression for derived values, (2) typed JMESPath object {"type":"jp"|"jmespath","expression":"..."}, or (3) typed CEL object {"type":"cel","expression":"..."}.
 */
export type From =
  | CaseExpression
  | CelExpression
  | (JMESPathExpression & { type: "jmespath" })
  | (JMESPathExpression & { type: "jp" })
  | string;

/**
 * One field-extraction mapping applied to a call action's tool response.
 *
 * @remarks
 *
 * Example: {"from": "is_valid", "to": "is_valid"}
 * Example (derived value): {"from": "status == 'valid'", "to": "is_valid"}
 */
export type CallResultSaveMapping = {
  /**
   * Expression evaluated against the parsed tool response body (the JSON the external tool returned). Supported forms: (1) JMESPath string (default for plain strings) — a bare field name for raw passthrough, or a boolean/computed expression for derived values, (2) typed JMESPath object {"type":"jp"|"jmespath","expression":"..."}, or (3) typed CEL object {"type":"cel","expression":"..."}.
   */
  from:
    | CaseExpression
    | CelExpression
    | (JMESPathExpression & { type: "jmespath" })
    | (JMESPathExpression & { type: "jp" })
    | string;
  /**
   * Workflow variable name to store the extracted value under.
   */
  to: string;
};

/** @internal */
export const From1$inboundSchema: z.ZodType<From1, z.ZodTypeDef, unknown> = z
  .union([
    CelExpression$inboundSchema,
    JMESPathExpression$inboundSchema.and(
      z.object({ type: z.literal("jmespath") }),
    ),
    JMESPathExpression$inboundSchema.and(z.object({ type: z.literal("jp") })),
  ]);
/** @internal */
export type From1$Outbound =
  | CelExpression$Outbound
  | (JMESPathExpression$Outbound & { type: "jmespath" })
  | (JMESPathExpression$Outbound & { type: "jp" });

/** @internal */
export const From1$outboundSchema: z.ZodType<
  From1$Outbound,
  z.ZodTypeDef,
  From1
> = z.union([
  CelExpression$outboundSchema,
  JMESPathExpression$outboundSchema.and(
    z.object({ type: z.literal("jmespath") }),
  ),
  JMESPathExpression$outboundSchema.and(z.object({ type: z.literal("jp") })),
]);

export function from1ToJSON(from1: From1): string {
  return JSON.stringify(From1$outboundSchema.parse(from1));
}
export function from1FromJSON(
  jsonString: string,
): SafeParseResult<From1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => From1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'From1' from JSON`,
  );
}

/** @internal */
export const From$inboundSchema: z.ZodType<From, z.ZodTypeDef, unknown> = z
  .union([
    CaseExpression$inboundSchema,
    z.union([
      CelExpression$inboundSchema,
      JMESPathExpression$inboundSchema.and(
        z.object({ type: z.literal("jmespath") }),
      ),
      JMESPathExpression$inboundSchema.and(z.object({ type: z.literal("jp") })),
    ]),
    z.string(),
  ]);
/** @internal */
export type From$Outbound =
  | CaseExpression$Outbound
  | CelExpression$Outbound
  | (JMESPathExpression$Outbound & { type: "jmespath" })
  | (JMESPathExpression$Outbound & { type: "jp" })
  | string;

/** @internal */
export const From$outboundSchema: z.ZodType<From$Outbound, z.ZodTypeDef, From> =
  z.union([
    CaseExpression$outboundSchema,
    z.union([
      CelExpression$outboundSchema,
      JMESPathExpression$outboundSchema.and(
        z.object({ type: z.literal("jmespath") }),
      ),
      JMESPathExpression$outboundSchema.and(
        z.object({ type: z.literal("jp") }),
      ),
    ]),
    z.string(),
  ]);

export function fromToJSON(from: From): string {
  return JSON.stringify(From$outboundSchema.parse(from));
}
export function fromFromJSON(
  jsonString: string,
): SafeParseResult<From, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => From$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'From' from JSON`,
  );
}

/** @internal */
export const CallResultSaveMapping$inboundSchema: z.ZodType<
  CallResultSaveMapping,
  z.ZodTypeDef,
  unknown
> = z.object({
  from: z.union([
    CaseExpression$inboundSchema,
    z.union([
      CelExpression$inboundSchema,
      JMESPathExpression$inboundSchema.and(
        z.object({ type: z.literal("jmespath") }),
      ),
      JMESPathExpression$inboundSchema.and(z.object({ type: z.literal("jp") })),
    ]),
    z.string(),
  ]),
  to: z.string(),
});
/** @internal */
export type CallResultSaveMapping$Outbound = {
  from:
    | CaseExpression$Outbound
    | CelExpression$Outbound
    | (JMESPathExpression$Outbound & { type: "jmespath" })
    | (JMESPathExpression$Outbound & { type: "jp" })
    | string;
  to: string;
};

/** @internal */
export const CallResultSaveMapping$outboundSchema: z.ZodType<
  CallResultSaveMapping$Outbound,
  z.ZodTypeDef,
  CallResultSaveMapping
> = z.object({
  from: z.union([
    CaseExpression$outboundSchema,
    z.union([
      CelExpression$outboundSchema,
      JMESPathExpression$outboundSchema.and(
        z.object({ type: z.literal("jmespath") }),
      ),
      JMESPathExpression$outboundSchema.and(
        z.object({ type: z.literal("jp") }),
      ),
    ]),
    z.string(),
  ]),
  to: z.string(),
});

export function callResultSaveMappingToJSON(
  callResultSaveMapping: CallResultSaveMapping,
): string {
  return JSON.stringify(
    CallResultSaveMapping$outboundSchema.parse(callResultSaveMapping),
  );
}
export function callResultSaveMappingFromJSON(
  jsonString: string,
): SafeParseResult<CallResultSaveMapping, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CallResultSaveMapping$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CallResultSaveMapping' from JSON`,
  );
}
