/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Outreach window and/or daily cap for one weekday or one calendar date.
 *
 * @remarks
 *
 * Every field may be omitted, and an omitted field inherits the campaign default: an override that
 * only sets ``max_daily_calls`` keeps the campaign window, and one that only sets the times keeps
 * the campaign cap. ``max_daily_calls: 0`` is not inheritance -- it is an explicit "no outreach on
 * this day", which is the shape the Outbound Scheduler daily_limit config already uses. It closes
 * the day for retries as well as for first attempts, the same way ``active: false`` does; a
 * non-zero maximum bounds first attempts only and lets retries run on.
 */
export type ScheduleOverride = {
  /**
   * Whether the campaign does outreach on this day. Omitted inherits: a weekday rule inherits from active_days, a date rule inherits from the weekday rule. Set it to false to close one date, for example a holiday, without a change to active_days.
   */
  active?: boolean | null | undefined;
  /**
   * Start time on this day. Omitted keeps the campaign start time.
   */
  dailyStartTime?: string | null | undefined;
  /**
   * End time on this day. Omitted keeps the campaign end time.
   */
  dailyEndTime?: string | null | undefined;
  /**
   * Maximum number of unique reference IDs that get outreach on this day. Omitted keeps the campaign maximum. Retries do not count towards it and keep dialing once it is reached. 0 is different: it closes the day for all outreach, retries included.
   */
  maxDailyCalls?: number | null | undefined;
};

/** @internal */
export const ScheduleOverride$inboundSchema: z.ZodType<
  ScheduleOverride,
  z.ZodTypeDef,
  unknown
> = z.object({
  active: z.nullable(z.boolean()).optional(),
  daily_start_time: z.nullable(z.string()).optional(),
  daily_end_time: z.nullable(z.string()).optional(),
  max_daily_calls: z.nullable(z.number().int()).optional(),
}).transform((v) => {
  return remap$(v, {
    "daily_start_time": "dailyStartTime",
    "daily_end_time": "dailyEndTime",
    "max_daily_calls": "maxDailyCalls",
  });
});
/** @internal */
export type ScheduleOverride$Outbound = {
  active?: boolean | null | undefined;
  daily_start_time?: string | null | undefined;
  daily_end_time?: string | null | undefined;
  max_daily_calls?: number | null | undefined;
};

/** @internal */
export const ScheduleOverride$outboundSchema: z.ZodType<
  ScheduleOverride$Outbound,
  z.ZodTypeDef,
  ScheduleOverride
> = z.object({
  active: z.nullable(z.boolean()).optional(),
  dailyStartTime: z.nullable(z.string()).optional(),
  dailyEndTime: z.nullable(z.string()).optional(),
  maxDailyCalls: z.nullable(z.number().int()).optional(),
}).transform((v) => {
  return remap$(v, {
    dailyStartTime: "daily_start_time",
    dailyEndTime: "daily_end_time",
    maxDailyCalls: "max_daily_calls",
  });
});

export function scheduleOverrideToJSON(
  scheduleOverride: ScheduleOverride,
): string {
  return JSON.stringify(
    ScheduleOverride$outboundSchema.parse(scheduleOverride),
  );
}
export function scheduleOverrideFromJSON(
  jsonString: string,
): SafeParseResult<ScheduleOverride, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduleOverride$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduleOverride' from JSON`,
  );
}
