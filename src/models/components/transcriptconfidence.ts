/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TranscriptWordConfidence,
  TranscriptWordConfidence$inboundSchema,
  TranscriptWordConfidence$Outbound,
  TranscriptWordConfidence$outboundSchema,
} from "./transcriptwordconfidence.js";

export const UtteranceSource = {
  Provider: "provider",
  DerivedMin: "derived_min",
} as const;
export type UtteranceSource = ClosedEnum<typeof UtteranceSource>;

/**
 * The STT provider's confidence for one user utterance.
 *
 * @remarks
 *
 * Provenance travels with the score because confidence is NOT comparable across providers:
 * Deepgram publishes a genuine acoustic confidence, while Google's proto disclaims the field and
 * Chirp 2's documentation states the value "isn't truly a confidence score". A bare float read
 * without knowing which backend produced it cannot be interpreted.
 *
 * `utterance_source` distinguishes a number the provider reported for exactly this text from one
 * we computed -- the weakest word, or the weakest across a merged turn. Absent entirely when the
 * backend reports nothing, so the transcript flow is unaffected. See ZOO-8595.
 */
export type TranscriptConfidence = {
  provider: string;
  model?: string | null | undefined;
  utterance?: number | null | undefined;
  utteranceSource?: UtteranceSource | null | undefined;
  words?: Array<TranscriptWordConfidence> | null | undefined;
  wordsTruncated?: boolean | null | undefined;
};

/** @internal */
export const UtteranceSource$inboundSchema: z.ZodNativeEnum<
  typeof UtteranceSource
> = z.nativeEnum(UtteranceSource);
/** @internal */
export const UtteranceSource$outboundSchema: z.ZodNativeEnum<
  typeof UtteranceSource
> = UtteranceSource$inboundSchema;

/** @internal */
export const TranscriptConfidence$inboundSchema: z.ZodType<
  TranscriptConfidence,
  z.ZodTypeDef,
  unknown
> = z.object({
  provider: z.string(),
  model: z.nullable(z.string()).optional(),
  utterance: z.nullable(z.number()).optional(),
  utterance_source: z.nullable(UtteranceSource$inboundSchema).optional(),
  words: z.nullable(z.array(TranscriptWordConfidence$inboundSchema)).optional(),
  words_truncated: z.nullable(z.boolean()).optional(),
}).transform((v) => {
  return remap$(v, {
    "utterance_source": "utteranceSource",
    "words_truncated": "wordsTruncated",
  });
});
/** @internal */
export type TranscriptConfidence$Outbound = {
  provider: string;
  model?: string | null | undefined;
  utterance?: number | null | undefined;
  utterance_source?: string | null | undefined;
  words?: Array<TranscriptWordConfidence$Outbound> | null | undefined;
  words_truncated?: boolean | null | undefined;
};

/** @internal */
export const TranscriptConfidence$outboundSchema: z.ZodType<
  TranscriptConfidence$Outbound,
  z.ZodTypeDef,
  TranscriptConfidence
> = z.object({
  provider: z.string(),
  model: z.nullable(z.string()).optional(),
  utterance: z.nullable(z.number()).optional(),
  utteranceSource: z.nullable(UtteranceSource$outboundSchema).optional(),
  words: z.nullable(z.array(TranscriptWordConfidence$outboundSchema))
    .optional(),
  wordsTruncated: z.nullable(z.boolean()).optional(),
}).transform((v) => {
  return remap$(v, {
    utteranceSource: "utterance_source",
    wordsTruncated: "words_truncated",
  });
});

export function transcriptConfidenceToJSON(
  transcriptConfidence: TranscriptConfidence,
): string {
  return JSON.stringify(
    TranscriptConfidence$outboundSchema.parse(transcriptConfidence),
  );
}
export function transcriptConfidenceFromJSON(
  jsonString: string,
): SafeParseResult<TranscriptConfidence, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TranscriptConfidence$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TranscriptConfidence' from JSON`,
  );
}
