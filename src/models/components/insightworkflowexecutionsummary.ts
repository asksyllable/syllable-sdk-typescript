/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * How many of a workflow's executions are in each status, optionally within a
 *
 * @remarks
 * created_at date range. Counts cover all matching rows, not just one page.
 */
export type InsightWorkflowExecutionSummary = {
  /**
   * Rows in PENDING status
   */
  pending?: number | undefined;
  /**
   * Rows in PROCESSING status
   */
  processing?: number | undefined;
  /**
   * Rows in COMPLETED status
   */
  completed?: number | undefined;
  /**
   * Rows in FAILED status
   */
  failed?: number | undefined;
  /**
   * Total rows across all statuses
   */
  total?: number | undefined;
};

/** @internal */
export const InsightWorkflowExecutionSummary$inboundSchema: z.ZodType<
  InsightWorkflowExecutionSummary,
  z.ZodTypeDef,
  unknown
> = z.object({
  pending: z.number().int().default(0),
  processing: z.number().int().default(0),
  completed: z.number().int().default(0),
  failed: z.number().int().default(0),
  total: z.number().int().default(0),
});
/** @internal */
export type InsightWorkflowExecutionSummary$Outbound = {
  pending: number;
  processing: number;
  completed: number;
  failed: number;
  total: number;
};

/** @internal */
export const InsightWorkflowExecutionSummary$outboundSchema: z.ZodType<
  InsightWorkflowExecutionSummary$Outbound,
  z.ZodTypeDef,
  InsightWorkflowExecutionSummary
> = z.object({
  pending: z.number().int().default(0),
  processing: z.number().int().default(0),
  completed: z.number().int().default(0),
  failed: z.number().int().default(0),
  total: z.number().int().default(0),
});

export function insightWorkflowExecutionSummaryToJSON(
  insightWorkflowExecutionSummary: InsightWorkflowExecutionSummary,
): string {
  return JSON.stringify(
    InsightWorkflowExecutionSummary$outboundSchema.parse(
      insightWorkflowExecutionSummary,
    ),
  );
}
export function insightWorkflowExecutionSummaryFromJSON(
  jsonString: string,
): SafeParseResult<InsightWorkflowExecutionSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InsightWorkflowExecutionSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InsightWorkflowExecutionSummary' from JSON`,
  );
}
