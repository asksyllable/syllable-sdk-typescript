/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DisplayCardMapping,
  DisplayCardMapping$inboundSchema,
  DisplayCardMapping$Outbound,
  DisplayCardMapping$outboundSchema,
} from "./displaycardmapping.js";

/**
 * Declarative presentation metadata stored with a tool definition.
 */
export type ResponseDisplayConfig = {
  version?: 1 | undefined;
  type: "card-list";
  itemsPath: string;
  /**
   * Supported mappings from one result item to the existing card shape.
   */
  mapping: DisplayCardMapping;
};

/** @internal */
export const ResponseDisplayConfig$inboundSchema: z.ZodType<
  ResponseDisplayConfig,
  z.ZodTypeDef,
  unknown
> = z.object({
  version: z.literal(1).default(1),
  type: z.literal("card-list"),
  items_path: z.string(),
  mapping: DisplayCardMapping$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "items_path": "itemsPath",
  });
});
/** @internal */
export type ResponseDisplayConfig$Outbound = {
  version: 1;
  type: "card-list";
  items_path: string;
  mapping: DisplayCardMapping$Outbound;
};

/** @internal */
export const ResponseDisplayConfig$outboundSchema: z.ZodType<
  ResponseDisplayConfig$Outbound,
  z.ZodTypeDef,
  ResponseDisplayConfig
> = z.object({
  version: z.literal(1).default(1 as const),
  type: z.literal("card-list"),
  itemsPath: z.string(),
  mapping: DisplayCardMapping$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    itemsPath: "items_path",
  });
});

export function responseDisplayConfigToJSON(
  responseDisplayConfig: ResponseDisplayConfig,
): string {
  return JSON.stringify(
    ResponseDisplayConfig$outboundSchema.parse(responseDisplayConfig),
  );
}
export function responseDisplayConfigFromJSON(
  jsonString: string,
): SafeParseResult<ResponseDisplayConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseDisplayConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseDisplayConfig' from JSON`,
  );
}
