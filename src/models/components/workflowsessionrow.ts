/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One session under a workflow: its execution status plus the insight results,
 *
 * @remarks
 * grouped by tool name and then by ``insight_key`` in the ``results`` dict.
 *
 * A session that is still pending, processing, or failed has an empty
 * ``results`` dict; ``status`` and ``error_message`` say why.
 */
export type WorkflowSessionRow = {
  /**
   * Session ID
   */
  sessionId: number;
  /**
   * When the workflow execution started processing this session; null while the row is still PENDING
   */
  analyzedAt?: Date | null | undefined;
  /**
   * When the session was queued for this workflow
   */
  queuedAt: Date;
  /**
   * Execution status of this session under the workflow
   */
  status: string;
  /**
   * Error message from the last processing attempt, if any
   */
  errorMessage?: string | null | undefined;
  /**
   * Results per tool, keyed by tool name then insight_key. Each insight_key holds a list of values: a single value comes back as a one-item list.
   */
  results?: { [k: string]: { [k: string]: Array<any> } } | undefined;
};

/** @internal */
export const WorkflowSessionRow$inboundSchema: z.ZodType<
  WorkflowSessionRow,
  z.ZodTypeDef,
  unknown
> = z.object({
  session_id: z.number().int(),
  analyzed_at: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ).optional(),
  queued_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  status: z.string(),
  error_message: z.nullable(z.string()).optional(),
  results: z.record(z.record(z.array(z.any()))).optional(),
}).transform((v) => {
  return remap$(v, {
    "session_id": "sessionId",
    "analyzed_at": "analyzedAt",
    "queued_at": "queuedAt",
    "error_message": "errorMessage",
  });
});
/** @internal */
export type WorkflowSessionRow$Outbound = {
  session_id: number;
  analyzed_at?: string | null | undefined;
  queued_at: string;
  status: string;
  error_message?: string | null | undefined;
  results?: { [k: string]: { [k: string]: Array<any> } } | undefined;
};

/** @internal */
export const WorkflowSessionRow$outboundSchema: z.ZodType<
  WorkflowSessionRow$Outbound,
  z.ZodTypeDef,
  WorkflowSessionRow
> = z.object({
  sessionId: z.number().int(),
  analyzedAt: z.nullable(z.date().transform(v => v.toISOString())).optional(),
  queuedAt: z.date().transform(v => v.toISOString()),
  status: z.string(),
  errorMessage: z.nullable(z.string()).optional(),
  results: z.record(z.record(z.array(z.any()))).optional(),
}).transform((v) => {
  return remap$(v, {
    sessionId: "session_id",
    analyzedAt: "analyzed_at",
    queuedAt: "queued_at",
    errorMessage: "error_message",
  });
});

export function workflowSessionRowToJSON(
  workflowSessionRow: WorkflowSessionRow,
): string {
  return JSON.stringify(
    WorkflowSessionRow$outboundSchema.parse(workflowSessionRow),
  );
}
export function workflowSessionRowFromJSON(
  jsonString: string,
): SafeParseResult<WorkflowSessionRow, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WorkflowSessionRow$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WorkflowSessionRow' from JSON`,
  );
}
