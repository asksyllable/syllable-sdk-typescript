/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TimelineEvent,
  TimelineEvent$inboundSchema,
  TimelineEvent$Outbound,
  TimelineEvent$outboundSchema,
} from "./timelineevent.js";

/**
 * Consolidated, time-ordered timeline of a session's events.
 */
export type SessionTimelineResponse = {
  /**
   * Internal ID of the session
   */
  sessionId: string;
  /**
   * Timestamp of the first event; the zero point for all offsets
   */
  sessionStart?: Date | null | undefined;
  /**
   * All events, ordered by timestamp ascending
   */
  events: Array<TimelineEvent>;
};

/** @internal */
export const SessionTimelineResponse$inboundSchema: z.ZodType<
  SessionTimelineResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  session_id: z.string(),
  session_start: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ).optional(),
  events: z.array(TimelineEvent$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "session_id": "sessionId",
    "session_start": "sessionStart",
  });
});
/** @internal */
export type SessionTimelineResponse$Outbound = {
  session_id: string;
  session_start?: string | null | undefined;
  events: Array<TimelineEvent$Outbound>;
};

/** @internal */
export const SessionTimelineResponse$outboundSchema: z.ZodType<
  SessionTimelineResponse$Outbound,
  z.ZodTypeDef,
  SessionTimelineResponse
> = z.object({
  sessionId: z.string(),
  sessionStart: z.nullable(z.date().transform(v => v.toISOString())).optional(),
  events: z.array(TimelineEvent$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    sessionId: "session_id",
    sessionStart: "session_start",
  });
});

export function sessionTimelineResponseToJSON(
  sessionTimelineResponse: SessionTimelineResponse,
): string {
  return JSON.stringify(
    SessionTimelineResponse$outboundSchema.parse(sessionTimelineResponse),
  );
}
export function sessionTimelineResponseFromJSON(
  jsonString: string,
): SafeParseResult<SessionTimelineResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionTimelineResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionTimelineResponse' from JSON`,
  );
}
