/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One uploaded file under a workflow: its execution status plus the insight
 *
 * @remarks
 * results, grouped by tool name and then by ``insight_key`` in the ``results``
 * dict.
 *
 * A file that is still pending, processing, or failed has an empty ``results``
 * dict; ``status`` and ``error_message`` say why.
 */
export type WorkflowFileRow = {
  /**
   * Upload file ID
   */
  uploadFileId: number;
  /**
   * When the workflow execution started processing this file; null while the row is still PENDING
   */
  analyzedAt?: Date | null | undefined;
  /**
   * When the file was queued for this workflow
   */
  queuedAt: Date;
  /**
   * Execution status of this file under the workflow
   */
  status: string;
  /**
   * Error message from the last processing attempt, if any
   */
  errorMessage?: string | null | undefined;
  /**
   * Results per tool, keyed by tool name then insight_key. Each insight_key holds a list of values: a single value comes back as a one-item list.
   */
  results?: { [k: string]: { [k: string]: Array<any> } } | undefined;
};

/** @internal */
export const WorkflowFileRow$inboundSchema: z.ZodType<
  WorkflowFileRow,
  z.ZodTypeDef,
  unknown
> = z.object({
  upload_file_id: z.number().int(),
  analyzed_at: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ).optional(),
  queued_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  status: z.string(),
  error_message: z.nullable(z.string()).optional(),
  results: z.record(z.record(z.array(z.any()))).optional(),
}).transform((v) => {
  return remap$(v, {
    "upload_file_id": "uploadFileId",
    "analyzed_at": "analyzedAt",
    "queued_at": "queuedAt",
    "error_message": "errorMessage",
  });
});
/** @internal */
export type WorkflowFileRow$Outbound = {
  upload_file_id: number;
  analyzed_at?: string | null | undefined;
  queued_at: string;
  status: string;
  error_message?: string | null | undefined;
  results?: { [k: string]: { [k: string]: Array<any> } } | undefined;
};

/** @internal */
export const WorkflowFileRow$outboundSchema: z.ZodType<
  WorkflowFileRow$Outbound,
  z.ZodTypeDef,
  WorkflowFileRow
> = z.object({
  uploadFileId: z.number().int(),
  analyzedAt: z.nullable(z.date().transform(v => v.toISOString())).optional(),
  queuedAt: z.date().transform(v => v.toISOString()),
  status: z.string(),
  errorMessage: z.nullable(z.string()).optional(),
  results: z.record(z.record(z.array(z.any()))).optional(),
}).transform((v) => {
  return remap$(v, {
    uploadFileId: "upload_file_id",
    analyzedAt: "analyzed_at",
    queuedAt: "queued_at",
    errorMessage: "error_message",
  });
});

export function workflowFileRowToJSON(
  workflowFileRow: WorkflowFileRow,
): string {
  return JSON.stringify(WorkflowFileRow$outboundSchema.parse(workflowFileRow));
}
export function workflowFileRowFromJSON(
  jsonString: string,
): SafeParseResult<WorkflowFileRow, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WorkflowFileRow$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WorkflowFileRow' from JSON`,
  );
}
