/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The type of action triggers, either "action", or "cta".
 */
export const ActionType = {
  Action: "action",
  Cta: "cta",
} as const;
/**
 * The type of action triggers, either "action", or "cta".
 */
export type ActionType = ClosedEnum<typeof ActionType>;

export type CardClickRequest = {
  /**
   * Human-readable identifier for the card action.
   */
  actionId: string;
  /**
   * The type of action triggers, either "action", or "cta".
   */
  actionType: ActionType;
};

/** @internal */
export const ActionType$inboundSchema: z.ZodNativeEnum<typeof ActionType> = z
  .nativeEnum(ActionType);
/** @internal */
export const ActionType$outboundSchema: z.ZodNativeEnum<typeof ActionType> =
  ActionType$inboundSchema;

/** @internal */
export const CardClickRequest$inboundSchema: z.ZodType<
  CardClickRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  action_id: z.string(),
  action_type: ActionType$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "action_id": "actionId",
    "action_type": "actionType",
  });
});
/** @internal */
export type CardClickRequest$Outbound = {
  action_id: string;
  action_type: string;
};

/** @internal */
export const CardClickRequest$outboundSchema: z.ZodType<
  CardClickRequest$Outbound,
  z.ZodTypeDef,
  CardClickRequest
> = z.object({
  actionId: z.string(),
  actionType: ActionType$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    actionId: "action_id",
    actionType: "action_type",
  });
});

export function cardClickRequestToJSON(
  cardClickRequest: CardClickRequest,
): string {
  return JSON.stringify(
    CardClickRequest$outboundSchema.parse(cardClickRequest),
  );
}
export function cardClickRequestFromJSON(
  jsonString: string,
): SafeParseResult<CardClickRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CardClickRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CardClickRequest' from JSON`,
  );
}
