/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One variant of an experiment as it is written.
 *
 * @remarks
 *
 * Variants are always written as a complete set, because a weight only means anything next to the
 * other weights, so there is no model for writing one on its own. The fields are checked by
 * `ExperimentsDao._validate_variants` and not by the model, so that every rule about a variant
 * reads in one place.
 */
export type DaoExperimentVariantWrite = {
  /**
   * The name of the variant, unique within the experiment
   */
  name: string;
  /**
   * The relative weight of the variant, not a percentage. (E.g., if two variants both have a weight of 10, they are equally likely to be selected.)
   */
  weight: number;
  /**
   * Whether this is the reference variant the others are measured against
   */
  isControl?: boolean | undefined;
  /**
   * The agent this variant runs
   */
  agentId: number;
};

/** @internal */
export const DaoExperimentVariantWrite$inboundSchema: z.ZodType<
  DaoExperimentVariantWrite,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string(),
  weight: z.number().int(),
  is_control: z.boolean().default(false),
  agent_id: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "is_control": "isControl",
    "agent_id": "agentId",
  });
});
/** @internal */
export type DaoExperimentVariantWrite$Outbound = {
  name: string;
  weight: number;
  is_control: boolean;
  agent_id: number;
};

/** @internal */
export const DaoExperimentVariantWrite$outboundSchema: z.ZodType<
  DaoExperimentVariantWrite$Outbound,
  z.ZodTypeDef,
  DaoExperimentVariantWrite
> = z.object({
  name: z.string(),
  weight: z.number().int(),
  isControl: z.boolean().default(false),
  agentId: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    isControl: "is_control",
    agentId: "agent_id",
  });
});

export function daoExperimentVariantWriteToJSON(
  daoExperimentVariantWrite: DaoExperimentVariantWrite,
): string {
  return JSON.stringify(
    DaoExperimentVariantWrite$outboundSchema.parse(daoExperimentVariantWrite),
  );
}
export function daoExperimentVariantWriteFromJSON(
  jsonString: string,
): SafeParseResult<DaoExperimentVariantWrite, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DaoExperimentVariantWrite$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DaoExperimentVariantWrite' from JSON`,
  );
}
