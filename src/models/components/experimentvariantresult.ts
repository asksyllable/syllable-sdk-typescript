/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * How much traffic one variant of an experiment handled.
 */
export type ExperimentVariantResult = {
  /**
   * The internal ID of the variant
   */
  variantId: number;
  /**
   * How many sessions this variant answered
   */
  sessionCount: number;
};

/** @internal */
export const ExperimentVariantResult$inboundSchema: z.ZodType<
  ExperimentVariantResult,
  z.ZodTypeDef,
  unknown
> = z.object({
  variant_id: z.number().int(),
  session_count: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "variant_id": "variantId",
    "session_count": "sessionCount",
  });
});
/** @internal */
export type ExperimentVariantResult$Outbound = {
  variant_id: number;
  session_count: number;
};

/** @internal */
export const ExperimentVariantResult$outboundSchema: z.ZodType<
  ExperimentVariantResult$Outbound,
  z.ZodTypeDef,
  ExperimentVariantResult
> = z.object({
  variantId: z.number().int(),
  sessionCount: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    variantId: "variant_id",
    sessionCount: "session_count",
  });
});

export function experimentVariantResultToJSON(
  experimentVariantResult: ExperimentVariantResult,
): string {
  return JSON.stringify(
    ExperimentVariantResult$outboundSchema.parse(experimentVariantResult),
  );
}
export function experimentVariantResultFromJSON(
  jsonString: string,
): SafeParseResult<ExperimentVariantResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExperimentVariantResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExperimentVariantResult' from JSON`,
  );
}
