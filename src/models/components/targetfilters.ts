/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TargetFilterRule,
  TargetFilterRule$inboundSchema,
  TargetFilterRule$Outbound,
  TargetFilterRule$outboundSchema,
} from "./targetfilterrule.js";

/**
 * How to combine rules: 'all' (AND) or 'any' (OR).
 */
export const Match = {
  All: "all",
  Any: "any",
} as const;
/**
 * How to combine rules: 'all' (AND) or 'any' (OR).
 */
export type Match = ClosedEnum<typeof Match>;

/**
 * Whether to dial requests whose enrichment is unresolved (no lookup data).
 */
export const OnUnknown = {
  Include: "include",
  Exclude: "exclude",
} as const;
/**
 * Whether to dial requests whose enrichment is unresolved (no lookup data).
 */
export type OnUnknown = ClosedEnum<typeof OnUnknown>;

/**
 * Generic target filter for an outbound campaign: a flat list of rules over request enrichment.
 *
 * @remarks
 *
 * A request is *dialable* when it matches the ruleset (``match='all'`` -> every rule; ``'any'`` ->
 * at least one rule); non-matching requests are skipped. ``on_unknown`` decides the fate of
 * requests whose enrichment never resolved (no lookup / lookup failed). An empty ``rules`` list
 * means no filter (all numbers dialed), matching the pre-filter behavior.
 */
export type TargetFilters = {
  /**
   * How to combine rules: 'all' (AND) or 'any' (OR).
   */
  match?: Match | undefined;
  /**
   * Whether to dial requests whose enrichment is unresolved (no lookup data).
   */
  onUnknown?: OnUnknown | undefined;
  /**
   * Predicates over request enrichment attributes. Empty means no filter.
   */
  rules?: Array<TargetFilterRule> | undefined;
};

/** @internal */
export const Match$inboundSchema: z.ZodNativeEnum<typeof Match> = z.nativeEnum(
  Match,
);
/** @internal */
export const Match$outboundSchema: z.ZodNativeEnum<typeof Match> =
  Match$inboundSchema;

/** @internal */
export const OnUnknown$inboundSchema: z.ZodNativeEnum<typeof OnUnknown> = z
  .nativeEnum(OnUnknown);
/** @internal */
export const OnUnknown$outboundSchema: z.ZodNativeEnum<typeof OnUnknown> =
  OnUnknown$inboundSchema;

/** @internal */
export const TargetFilters$inboundSchema: z.ZodType<
  TargetFilters,
  z.ZodTypeDef,
  unknown
> = z.object({
  match: Match$inboundSchema.default("all"),
  on_unknown: OnUnknown$inboundSchema.default("include"),
  rules: z.array(TargetFilterRule$inboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    "on_unknown": "onUnknown",
  });
});
/** @internal */
export type TargetFilters$Outbound = {
  match: string;
  on_unknown: string;
  rules?: Array<TargetFilterRule$Outbound> | undefined;
};

/** @internal */
export const TargetFilters$outboundSchema: z.ZodType<
  TargetFilters$Outbound,
  z.ZodTypeDef,
  TargetFilters
> = z.object({
  match: Match$outboundSchema.default("all"),
  onUnknown: OnUnknown$outboundSchema.default("include"),
  rules: z.array(TargetFilterRule$outboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    onUnknown: "on_unknown",
  });
});

export function targetFiltersToJSON(targetFilters: TargetFilters): string {
  return JSON.stringify(TargetFilters$outboundSchema.parse(targetFilters));
}
export function targetFiltersFromJSON(
  jsonString: string,
): SafeParseResult<TargetFilters, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TargetFilters$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TargetFilters' from JSON`,
  );
}
