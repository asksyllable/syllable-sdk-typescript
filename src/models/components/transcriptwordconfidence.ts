/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Per-word STT confidence. `word` prefers the provider's punctuated form so the stored words
 *
 * @remarks
 * read the same way as the transcript they came from.
 */
export type TranscriptWordConfidence = {
  word: string;
  confidence?: number | null | undefined;
  start?: number | null | undefined;
  end?: number | null | undefined;
};

/** @internal */
export const TranscriptWordConfidence$inboundSchema: z.ZodType<
  TranscriptWordConfidence,
  z.ZodTypeDef,
  unknown
> = z.object({
  word: z.string(),
  confidence: z.nullable(z.number()).optional(),
  start: z.nullable(z.number()).optional(),
  end: z.nullable(z.number()).optional(),
});
/** @internal */
export type TranscriptWordConfidence$Outbound = {
  word: string;
  confidence?: number | null | undefined;
  start?: number | null | undefined;
  end?: number | null | undefined;
};

/** @internal */
export const TranscriptWordConfidence$outboundSchema: z.ZodType<
  TranscriptWordConfidence$Outbound,
  z.ZodTypeDef,
  TranscriptWordConfidence
> = z.object({
  word: z.string(),
  confidence: z.nullable(z.number()).optional(),
  start: z.nullable(z.number()).optional(),
  end: z.nullable(z.number()).optional(),
});

export function transcriptWordConfidenceToJSON(
  transcriptWordConfidence: TranscriptWordConfidence,
): string {
  return JSON.stringify(
    TranscriptWordConfidence$outboundSchema.parse(transcriptWordConfidence),
  );
}
export function transcriptWordConfidenceFromJSON(
  jsonString: string,
): SafeParseResult<TranscriptWordConfidence, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TranscriptWordConfidence$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TranscriptWordConfidence' from JSON`,
  );
}
