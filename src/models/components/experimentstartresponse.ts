/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ExperimentResponse,
  ExperimentResponse$inboundSchema,
  ExperimentResponse$Outbound,
  ExperimentResponse$outboundSchema,
} from "./experimentresponse.js";

/**
 * A started experiment.
 */
export type ExperimentStartResponse = {
  /**
   * Response model for experiment operations.
   *
   * @remarks
   *
   * An experiment splits the traffic of one channel target between two or more agents, so that the
   * agents can be compared on the same population. One variant is the control the others are
   * measured against. Each variant is a weighted pointer to the agent that variant runs; the weights
   * are relative and not percentages.
   */
  experiment: ExperimentResponse;
  /**
   * Things that weaken the result without invalidating it, such as a variant whose agent does not pin a prompt version, so that the prompt can be edited while the experiment runs
   */
  warnings?: Array<string> | undefined;
};

/** @internal */
export const ExperimentStartResponse$inboundSchema: z.ZodType<
  ExperimentStartResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  experiment: ExperimentResponse$inboundSchema,
  warnings: z.array(z.string()).optional(),
});
/** @internal */
export type ExperimentStartResponse$Outbound = {
  experiment: ExperimentResponse$Outbound;
  warnings?: Array<string> | undefined;
};

/** @internal */
export const ExperimentStartResponse$outboundSchema: z.ZodType<
  ExperimentStartResponse$Outbound,
  z.ZodTypeDef,
  ExperimentStartResponse
> = z.object({
  experiment: ExperimentResponse$outboundSchema,
  warnings: z.array(z.string()).optional(),
});

export function experimentStartResponseToJSON(
  experimentStartResponse: ExperimentStartResponse,
): string {
  return JSON.stringify(
    ExperimentStartResponse$outboundSchema.parse(experimentStartResponse),
  );
}
export function experimentStartResponseFromJSON(
  jsonString: string,
): SafeParseResult<ExperimentStartResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExperimentStartResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExperimentStartResponse' from JSON`,
  );
}
