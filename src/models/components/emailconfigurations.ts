/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Email channel configurations.
 */
export type EmailConfigurations = {
  /**
   * The domain to use for sending emails. Must be authenticated in SendGrid.
   */
  sendingDomain: string;
};

/** @internal */
export const EmailConfigurations$inboundSchema: z.ZodType<
  EmailConfigurations,
  z.ZodTypeDef,
  unknown
> = z.object({
  sending_domain: z.string(),
}).transform((v) => {
  return remap$(v, {
    "sending_domain": "sendingDomain",
  });
});
/** @internal */
export type EmailConfigurations$Outbound = {
  sending_domain: string;
};

/** @internal */
export const EmailConfigurations$outboundSchema: z.ZodType<
  EmailConfigurations$Outbound,
  z.ZodTypeDef,
  EmailConfigurations
> = z.object({
  sendingDomain: z.string(),
}).transform((v) => {
  return remap$(v, {
    sendingDomain: "sending_domain",
  });
});

export function emailConfigurationsToJSON(
  emailConfigurations: EmailConfigurations,
): string {
  return JSON.stringify(
    EmailConfigurations$outboundSchema.parse(emailConfigurations),
  );
}
export function emailConfigurationsFromJSON(
  jsonString: string,
): SafeParseResult<EmailConfigurations, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EmailConfigurations$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EmailConfigurations' from JSON`,
  );
}
