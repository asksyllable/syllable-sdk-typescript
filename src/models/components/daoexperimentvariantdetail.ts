/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A variant, with the name of the agent it runs resolved for display.
 */
export type DaoExperimentVariantDetail = {
  /**
   * The internal ID of the variant
   */
  id: number;
  /**
   * The name of the variant, which is the grouping key in reports
   */
  name: string;
  /**
   * The relative weight of the variant, not a percentage. (E.g., if two variants both have a weight of 10, they are equally likely to be selected.)
   */
  weight: number;
  /**
   * Whether this is the reference variant the others are measured against
   */
  isControl: boolean;
  /**
   * The agent this variant runs
   */
  agentId: number;
  /**
   * The name of that agent
   */
  agentName: string;
};

/** @internal */
export const DaoExperimentVariantDetail$inboundSchema: z.ZodType<
  DaoExperimentVariantDetail,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.number().int(),
  name: z.string(),
  weight: z.number().int(),
  is_control: z.boolean(),
  agent_id: z.number().int(),
  agent_name: z.string(),
}).transform((v) => {
  return remap$(v, {
    "is_control": "isControl",
    "agent_id": "agentId",
    "agent_name": "agentName",
  });
});
/** @internal */
export type DaoExperimentVariantDetail$Outbound = {
  id: number;
  name: string;
  weight: number;
  is_control: boolean;
  agent_id: number;
  agent_name: string;
};

/** @internal */
export const DaoExperimentVariantDetail$outboundSchema: z.ZodType<
  DaoExperimentVariantDetail$Outbound,
  z.ZodTypeDef,
  DaoExperimentVariantDetail
> = z.object({
  id: z.number().int(),
  name: z.string(),
  weight: z.number().int(),
  isControl: z.boolean(),
  agentId: z.number().int(),
  agentName: z.string(),
}).transform((v) => {
  return remap$(v, {
    isControl: "is_control",
    agentId: "agent_id",
    agentName: "agent_name",
  });
});

export function daoExperimentVariantDetailToJSON(
  daoExperimentVariantDetail: DaoExperimentVariantDetail,
): string {
  return JSON.stringify(
    DaoExperimentVariantDetail$outboundSchema.parse(daoExperimentVariantDetail),
  );
}
export function daoExperimentVariantDetailFromJSON(
  jsonString: string,
): SafeParseResult<DaoExperimentVariantDetail, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DaoExperimentVariantDetail$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DaoExperimentVariantDetail' from JSON`,
  );
}
