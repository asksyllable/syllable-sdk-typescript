/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  LifecycleStatus,
  LifecycleStatus$inboundSchema,
  LifecycleStatus$outboundSchema,
} from "./lifecyclestatus.js";

/**
 * An STT provider offered to agents, with its resolved deprecation lifecycle.
 */
export type SupportedSttProvider = {
  /**
   * Speech-to-text provider value, as stored on the agent.
   */
  provider: string;
  /**
   * Human-readable name of the provider.
   */
  displayName: string;
  /**
   * Whether the provider is in the deprecation warning window.
   */
  deprecated?: boolean | undefined;
  /**
   * Date the provider becomes retired and can no longer be saved or selected.
   */
  sunsetDate?: RFCDate | null | undefined;
  /**
   * Whether the provider is force-retired regardless of sunset_date.
   */
  removed?: boolean | undefined;
  /**
   * Provider substituted at runtime for critical features once this one is retired.
   */
  fallback?: string | null | undefined;
  /**
   * Effective lifecycle status, resolved server-side against the current date.
   */
  status?: LifecycleStatus | null | undefined;
};

/** @internal */
export const SupportedSttProvider$inboundSchema: z.ZodType<
  SupportedSttProvider,
  z.ZodTypeDef,
  unknown
> = z.object({
  provider: z.string(),
  display_name: z.string(),
  deprecated: z.boolean().default(false),
  sunset_date: z.nullable(z.string().transform(v => new RFCDate(v))).optional(),
  removed: z.boolean().default(false),
  fallback: z.nullable(z.string()).optional(),
  status: z.nullable(LifecycleStatus$inboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    "display_name": "displayName",
    "sunset_date": "sunsetDate",
  });
});
/** @internal */
export type SupportedSttProvider$Outbound = {
  provider: string;
  display_name: string;
  deprecated: boolean;
  sunset_date?: string | null | undefined;
  removed: boolean;
  fallback?: string | null | undefined;
  status?: string | null | undefined;
};

/** @internal */
export const SupportedSttProvider$outboundSchema: z.ZodType<
  SupportedSttProvider$Outbound,
  z.ZodTypeDef,
  SupportedSttProvider
> = z.object({
  provider: z.string(),
  displayName: z.string(),
  deprecated: z.boolean().default(false),
  sunsetDate: z.nullable(z.instanceof(RFCDate).transform(v => v.toString()))
    .optional(),
  removed: z.boolean().default(false),
  fallback: z.nullable(z.string()).optional(),
  status: z.nullable(LifecycleStatus$outboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    displayName: "display_name",
    sunsetDate: "sunset_date",
  });
});

export function supportedSttProviderToJSON(
  supportedSttProvider: SupportedSttProvider,
): string {
  return JSON.stringify(
    SupportedSttProvider$outboundSchema.parse(supportedSttProvider),
  );
}
export function supportedSttProviderFromJSON(
  jsonString: string,
): SafeParseResult<SupportedSttProvider, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SupportedSttProvider$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SupportedSttProvider' from JSON`,
  );
}
