/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TargetModes,
  TargetModes$inboundSchema,
  TargetModes$outboundSchema,
} from "./targetmodes.js";

/**
 * The channel target an experiment splits, resolved for display.
 *
 * @remarks
 *
 * Enough to name the target on the experiment card without a second read: the Console shows the
 * target itself and the channel it belongs to, and links to neither by ID.
 */
export type DaoExperimentTargetSummary = {
  /**
   * The internal ID of the channel target
   */
  id: number;
  /**
   * The target itself, such as a phone number
   */
  target: string;
  /**
   * Available modes (communication methods) for channel targets.
   */
  targetMode: TargetModes;
  /**
   * The channel the target belongs to
   */
  channelId: number;
  /**
   * The name of that channel
   */
  channelName: string;
};

/** @internal */
export const DaoExperimentTargetSummary$inboundSchema: z.ZodType<
  DaoExperimentTargetSummary,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.number().int(),
  target: z.string(),
  target_mode: TargetModes$inboundSchema,
  channel_id: z.number().int(),
  channel_name: z.string(),
}).transform((v) => {
  return remap$(v, {
    "target_mode": "targetMode",
    "channel_id": "channelId",
    "channel_name": "channelName",
  });
});
/** @internal */
export type DaoExperimentTargetSummary$Outbound = {
  id: number;
  target: string;
  target_mode: string;
  channel_id: number;
  channel_name: string;
};

/** @internal */
export const DaoExperimentTargetSummary$outboundSchema: z.ZodType<
  DaoExperimentTargetSummary$Outbound,
  z.ZodTypeDef,
  DaoExperimentTargetSummary
> = z.object({
  id: z.number().int(),
  target: z.string(),
  targetMode: TargetModes$outboundSchema,
  channelId: z.number().int(),
  channelName: z.string(),
}).transform((v) => {
  return remap$(v, {
    targetMode: "target_mode",
    channelId: "channel_id",
    channelName: "channel_name",
  });
});

export function daoExperimentTargetSummaryToJSON(
  daoExperimentTargetSummary: DaoExperimentTargetSummary,
): string {
  return JSON.stringify(
    DaoExperimentTargetSummary$outboundSchema.parse(daoExperimentTargetSummary),
  );
}
export function daoExperimentTargetSummaryFromJSON(
  jsonString: string,
): SafeParseResult<DaoExperimentTargetSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DaoExperimentTargetSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DaoExperimentTargetSummary' from JSON`,
  );
}
