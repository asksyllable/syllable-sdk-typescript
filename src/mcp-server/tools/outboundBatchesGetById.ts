/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundBatchesGetById } from "../../funcs/outboundBatchesGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundBatchGetByIdRequest$inboundSchema,
};

export const tool$outboundBatchesGetById: ToolDefinition<typeof args> = {
  name: "outbound-batches-get-by-id",
  description: `Get Outbound Communication Batch`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundBatchesGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
