/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundBatchesResults } from "../../funcs/outboundBatchesResults.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundBatchResultsRequest$inboundSchema,
};

export const tool$outboundBatchesResults: ToolDefinition<typeof args> = {
  name: "outbound-batches-results",
  description: `Fetch Outbound Communication Batch Results`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundBatchesResults(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
