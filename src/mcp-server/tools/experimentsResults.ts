/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsResults } from "../../funcs/experimentsResults.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExperimentsResultsRequest$inboundSchema,
};

export const tool$experimentsResults: ToolDefinition<typeof args> = {
  name: "experiments-results",
  description: `Get Experiment Results

Get the number of sessions each variant of an experiment has handled.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsResults(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
