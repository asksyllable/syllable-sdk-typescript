/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionLabelsCreate } from "../../funcs/sessionLabelsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.SessionLabelCreate$inboundSchema,
};

export const tool$sessionLabelsCreate: ToolDefinition<typeof args> = {
  name: "session-labels-create",
  description: `Create Label

Create a new label`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionLabelsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
