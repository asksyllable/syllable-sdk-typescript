/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dashboardsPostListDashboard } from "../../funcs/dashboardsPostListDashboard.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostListDashboardRequest$inboundSchema,
};

export const tool$dashboardsPostListDashboard: ToolDefinition<typeof args> = {
  name: "dashboards-post-list-dashboard",
  description: `Post List Dashboards

METHOD: POST
URL: /dashboard/list
ARGUMENTS: None
RETURNS: List of dashboards`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dashboardsPostListDashboard(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
