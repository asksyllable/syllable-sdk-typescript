/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsFoldersListFiles } from "../../funcs/insightsFoldersListFiles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsUploadListFilesRequest$inboundSchema,
};

export const tool$insightsFoldersListFiles: ToolDefinition<typeof args> = {
  name: "insights-folders-list-files",
  description: `Fetch Insights Upload Files`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsFoldersListFiles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
