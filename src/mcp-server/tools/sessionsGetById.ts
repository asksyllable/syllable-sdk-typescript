/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsGetById } from "../../funcs/sessionsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SessionGetByIdRequest$inboundSchema,
};

export const tool$sessionsGetById: ToolDefinition<typeof args> = {
  name: "sessions-get-by-id",
  description: `Get A Single Session By Id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
