/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundCampaignsCreate } from "../../funcs/outboundCampaignsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.OutboundCampaignInput$inboundSchema,
};

export const tool$outboundCampaignsCreate: ToolDefinition<typeof args> = {
  name: "outbound-campaigns-create",
  description: `Create Outbound Communication Campaign`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundCampaignsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
