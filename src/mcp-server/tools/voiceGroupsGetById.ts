/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { voiceGroupsGetById } from "../../funcs/voiceGroupsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.VoiceGroupsGetByIdRequest$inboundSchema,
};

export const tool$voiceGroupsGetById: ToolDefinition<typeof args> = {
  name: "voice-groups-get-by-id",
  description: `Get Voice Group

Fetch a given voice group.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await voiceGroupsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
