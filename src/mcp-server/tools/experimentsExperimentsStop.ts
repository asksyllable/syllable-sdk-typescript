/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsExperimentsStop } from "../../funcs/experimentsExperimentsStop.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExperimentsStopRequest$inboundSchema,
};

export const tool$experimentsExperimentsStop: ToolDefinition<typeof args> = {
  name: "experiments-experiments-stop",
  description: `Stop Experiment

Stop a running experiment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsExperimentsStop(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
