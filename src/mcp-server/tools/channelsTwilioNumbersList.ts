/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTwilioNumbersList } from "../../funcs/channelsTwilioNumbersList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChannelsTwilioNumbersListRequest$inboundSchema,
};

export const tool$channelsTwilioNumbersList: ToolDefinition<typeof args> = {
  name: "channels-twilio-numbers-list",
  description: `List Twilio Phone Numbers

List Twilio phone numbers.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsTwilioNumbersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
