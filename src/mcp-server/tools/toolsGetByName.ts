/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsGetByName } from "../../funcs/toolsGetByName.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolGetByNameRequest$inboundSchema,
};

export const tool$toolsGetByName: ToolDefinition<typeof args> = {
  name: "tools-get-by-name",
  description: `Tool Info

Get the details of a specific tool`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsGetByName(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
