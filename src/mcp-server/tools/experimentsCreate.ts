/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsCreate } from "../../funcs/experimentsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.ExperimentCreateRequest$inboundSchema,
};

export const tool$experimentsCreate: ToolDefinition<typeof args> = {
  name: "experiments-create",
  description: `Create Experiment

Create a new experiment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
