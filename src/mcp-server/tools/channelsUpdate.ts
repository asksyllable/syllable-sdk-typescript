/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsUpdate } from "../../funcs/channelsUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.OrganizationChannelUpdateRequest$inboundSchema,
};

export const tool$channelsUpdate: ToolDefinition<typeof args> = {
  name: "channels-update",
  description: `Update Channel`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
