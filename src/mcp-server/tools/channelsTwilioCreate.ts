/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTwilioCreate } from "../../funcs/channelsTwilioCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.TwilioChannelCreateRequest$inboundSchema,
};

export const tool$channelsTwilioCreate: ToolDefinition<typeof args> = {
  name: "channels-twilio-create",
  description: `Create Twilio Channel`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsTwilioCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
