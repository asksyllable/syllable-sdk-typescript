/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataSourcesDelete } from "../../funcs/dataSourcesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DataSourcesDeleteRequest$inboundSchema,
};

export const tool$dataSourcesDelete: ToolDefinition<typeof args> = {
  name: "data-sources-delete",
  description: `Delete Data Source

Delete a given data source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataSourcesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
