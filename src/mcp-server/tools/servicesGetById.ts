/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { servicesGetById } from "../../funcs/servicesGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ServicesGetByIdRequest$inboundSchema,
};

export const tool$servicesGetById: ToolDefinition<typeof args> = {
  name: "services-get-by-id",
  description: `Get Service By Id

Get a service by its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await servicesGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
