/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dashboardsPostSessionsDashboard } from "../../funcs/dashboardsPostSessionsDashboard.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dashboardsPostSessionsDashboard: ToolDefinition = {
  name: "dashboards-post-sessions-dashboard",
  description: `Post Sessions

METHOD: POST
URL: /dashboard/sessions
ARGUMENTS: None
RETURNS: Dashboard info for embedding
DEPRECATED: This endpoint is deprecated. Please use /dashboard/list instead`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await dashboardsPostSessionsDashboard(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
