/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { promptsGetById } from "../../funcs/promptsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PromptsGetByIdRequest$inboundSchema,
};

export const tool$promptsGetById: ToolDefinition<typeof args> = {
  name: "prompts-get-by-id",
  description: `Get Prompt By Id

Get a prompt by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await promptsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
