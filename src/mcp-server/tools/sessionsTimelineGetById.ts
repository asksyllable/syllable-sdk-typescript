/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsTimelineGetById } from "../../funcs/sessionsTimelineGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SessionTimelineGetByIdRequest$inboundSchema,
};

export const tool$sessionsTimelineGetById: ToolDefinition<typeof args> = {
  name: "sessions-timeline-get-by-id",
  description: `Get Session Timeline By Id

Consolidated, time-ordered timeline of a session's events.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsTimelineGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
