/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionLabelsGetById } from "../../funcs/sessionLabelsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SessionLabelGetByIdRequest$inboundSchema,
};

export const tool$sessionLabelsGetById: ToolDefinition<typeof args> = {
  name: "session-labels-get-by-id",
  description: `Get Label By Id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionLabelsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
