/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsToolsInsightsToolTest } from "../../funcs/insightsToolsInsightsToolTest.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.InsightToolTestInput$inboundSchema,
};

export const tool$insightsToolsInsightsToolTest: ToolDefinition<typeof args> = {
  name: "insights-tools-insights-tool-test",
  description: `Test Insights Tool

Manually run the given insight tool against a session and return the response.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsToolsInsightsToolTest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
