/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsGenerateSessionRecordingUrls } from "../../funcs/sessionsGenerateSessionRecordingUrls.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GenerateSessionRecordingUrlsRequest$inboundSchema,
};

export const tool$sessionsGenerateSessionRecordingUrls: ToolDefinition<
  typeof args
> = {
  name: "sessions-generate-session-recording-urls",
  description: `Generate Recording Urls`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsGenerateSessionRecordingUrls(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
