/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { takeoutsTakeoutsGetByJobId } from "../../funcs/takeoutsTakeoutsGetByJobId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TakeoutsGetByJobIdRequest$inboundSchema,
};

export const tool$takeoutsTakeoutsGetByJobId: ToolDefinition<typeof args> = {
  name: "takeouts-takeouts-get-by-job-id",
  description: `Get Takeout`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await takeoutsTakeoutsGetByJobId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
