/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersUsersGetByEmail } from "../../funcs/usersUsersGetByEmail.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UsersGetByEmailRequest$inboundSchema,
};

export const tool$usersUsersGetByEmail: ToolDefinition<typeof args> = {
  name: "users-users-get-by-email",
  description: `Get User

Fetch a given user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersUsersGetByEmail(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
