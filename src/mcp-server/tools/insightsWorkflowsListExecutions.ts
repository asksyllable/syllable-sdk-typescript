/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsWorkflowsListExecutions } from "../../funcs/insightsWorkflowsListExecutions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsWorkflowExecutionsRequest$inboundSchema,
};

export const tool$insightsWorkflowsListExecutions: ToolDefinition<typeof args> =
  {
    name: "insights-workflows-list-executions",
    description: `List Insight Workflow Executions

List a workflow's execution rows.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await insightsWorkflowsListExecutions(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
