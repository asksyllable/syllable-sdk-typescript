/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsToolsUpdate } from "../../funcs/insightsToolsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsToolUpdateRequest$inboundSchema,
};

export const tool$insightsToolsUpdate: ToolDefinition<typeof args> = {
  name: "insights-tools-update",
  description: `Update Insights Tool Configuration

Update an Insights tool.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsToolsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
