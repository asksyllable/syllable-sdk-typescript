/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsSipIpRangesDelete } from "../../funcs/organizationsSipIpRangesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OrganizationSipIpRangesDeleteRequest$inboundSchema,
};

export const tool$organizationsSipIpRangesDelete: ToolDefinition<typeof args> =
  {
    name: "organizations-sip-ip-ranges-delete",
    description: `Delete Organization Sip Ip Range

Delete a SIP signaling/media IP range for the current organization.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await organizationsSipIpRangesDelete(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
