/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bridgePhrasesGetById } from "../../funcs/bridgePhrasesGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.BridgePhrasesGetByIdRequest$inboundSchema,
};

export const tool$bridgePhrasesGetById: ToolDefinition<typeof args> = {
  name: "bridge-phrases-get-by-id",
  description: `Get Bridge Phrases

Fetch a given bridge phrases config.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bridgePhrasesGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
