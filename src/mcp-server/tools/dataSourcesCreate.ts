/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataSourcesCreate } from "../../funcs/dataSourcesCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.DataSourceCreateRequest$inboundSchema,
};

export const tool$dataSourcesCreate: ToolDefinition<typeof args> = {
  name: "data-sources-create",
  description: `Create Data Source

Create a new data source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataSourcesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
