/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customMessagesGetById } from "../../funcs/customMessagesGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CustomMessageGetByIdRequest$inboundSchema,
};

export const tool$customMessagesGetById: ToolDefinition<typeof args> = {
  name: "custom-messages-get-by-id",
  description: `Get Custom Message By Id

Get the custom message by its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customMessagesGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
