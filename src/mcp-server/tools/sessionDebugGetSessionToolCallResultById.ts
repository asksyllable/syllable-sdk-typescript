/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionDebugGetSessionToolCallResultById } from "../../funcs/sessionDebugGetSessionToolCallResultById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSessionToolCallResultByIdRequest$inboundSchema,
};

export const tool$sessionDebugGetSessionToolCallResultById: ToolDefinition<
  typeof args
> = {
  name: "session-debug-get-session-tool-call-result-by-id",
  description: `Get Session Tool Call Result By Id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionDebugGetSessionToolCallResultById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
