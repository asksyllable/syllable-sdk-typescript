/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTargetsAvailableTargets } from "../../funcs/channelsTargetsAvailableTargets.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AvailableTargetsRequest$inboundSchema,
};

export const tool$channelsTargetsAvailableTargets: ToolDefinition<typeof args> =
  {
    name: "channels-targets-available-targets",
    description: `Available Targets List

List the available phone numbers`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await channelsTargetsAvailableTargets(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
