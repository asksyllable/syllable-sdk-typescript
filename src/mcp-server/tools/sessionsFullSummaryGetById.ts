/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsFullSummaryGetById } from "../../funcs/sessionsFullSummaryGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SessionFullSummaryGetByIdRequest$inboundSchema,
};

export const tool$sessionsFullSummaryGetById: ToolDefinition<typeof args> = {
  name: "sessions-full-summary-get-by-id",
  description: `Get Full Session Summary By Id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsFullSummaryGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
