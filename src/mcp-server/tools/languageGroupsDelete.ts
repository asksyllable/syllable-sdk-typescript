/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { languageGroupsDelete } from "../../funcs/languageGroupsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.LanguageGroupsDeleteRequest$inboundSchema,
};

export const tool$languageGroupsDelete: ToolDefinition<typeof args> = {
  name: "language-groups-delete",
  description: `Delete Language Group

Delete a language group.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageGroupsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
