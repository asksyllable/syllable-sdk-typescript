/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundBatchesDelete } from "../../funcs/outboundBatchesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundBatchDeleteRequest$inboundSchema,
};

export const tool$outboundBatchesDelete: ToolDefinition<typeof args> = {
  name: "outbound-batches-delete",
  description: `Delete Outbound Communication Batch`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundBatchesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
