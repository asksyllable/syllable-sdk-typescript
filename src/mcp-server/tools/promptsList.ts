/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { promptsList } from "../../funcs/promptsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PromptsListRequest$inboundSchema,
};

export const tool$promptsList: ToolDefinition<typeof args> = {
  name: "prompts-list",
  description: `Prompt List

List the existing prompts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await promptsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
