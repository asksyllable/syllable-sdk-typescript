/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsToolsList } from "../../funcs/insightsToolsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightToolListRequest$inboundSchema,
};

export const tool$insightsToolsList: ToolDefinition<typeof args> = {
  name: "insights-tools-list",
  description: `List Insight Tool Configurations

List the existing insight_tools`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsToolsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
