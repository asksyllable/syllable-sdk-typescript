/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationConfigGetBridgePhrasesConfig } from "../../funcs/conversationConfigGetBridgePhrasesConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBridgePhrasesConfigRequest$inboundSchema,
};

export const tool$conversationConfigGetBridgePhrasesConfig: ToolDefinition<
  typeof args
> = {
  name: "conversation-config-get-bridge-phrases-config",
  description: `Get Bridge Phrases Config

Get the bridge phrases configuration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationConfigGetBridgePhrasesConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
