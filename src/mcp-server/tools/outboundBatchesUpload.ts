/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundBatchesUpload } from "../../funcs/outboundBatchesUpload.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundBatchUploadRequest$inboundSchema,
};

export const tool$outboundBatchesUpload: ToolDefinition<typeof args> = {
  name: "outbound-batches-upload",
  description: `Upload Outbound Communication Batch`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundBatchesUpload(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
