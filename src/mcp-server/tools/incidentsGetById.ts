/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsGetById } from "../../funcs/incidentsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.IncidentGetByIdRequest$inboundSchema,
};

export const tool$incidentsGetById: ToolDefinition<typeof args> = {
  name: "incidents-get-by-id",
  description: `Get Incident By Id

Get incident by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
