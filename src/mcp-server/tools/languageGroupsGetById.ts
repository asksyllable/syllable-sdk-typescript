/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { languageGroupsGetById } from "../../funcs/languageGroupsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.LanguageGroupsGetByIdRequest$inboundSchema,
};

export const tool$languageGroupsGetById: ToolDefinition<typeof args> = {
  name: "language-groups-get-by-id",
  description: `Get Language Group

Deprecated alias for \`GET /api/v1/voice_groups/{voice_group_id}\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageGroupsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
