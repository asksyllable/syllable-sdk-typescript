/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rolesDelete } from "../../funcs/rolesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RolesDeleteRequest$inboundSchema,
};

export const tool$rolesDelete: ToolDefinition<typeof args> = {
  name: "roles-delete",
  description: `Delete Role

Delete a role. Optionally, reassign existing users with that role to a new role.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await rolesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
