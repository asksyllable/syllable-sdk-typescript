/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsGetById } from "../../funcs/agentsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AgentGetByIdRequest$inboundSchema,
};

export const tool$agentsGetById: ToolDefinition<typeof args> = {
  name: "agents-get-by-id",
  description: `Get Agent By Id

Get an agent by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
