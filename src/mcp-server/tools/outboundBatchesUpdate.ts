/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundBatchesUpdate } from "../../funcs/outboundBatchesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundBatchUpdateRequest$inboundSchema,
};

export const tool$outboundBatchesUpdate: ToolDefinition<typeof args> = {
  name: "outbound-batches-update",
  description: `Update Outbound Communication Batch`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundBatchesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
