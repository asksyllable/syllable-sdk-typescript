/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsWorkflowsExecutionsSummary } from "../../funcs/insightsWorkflowsExecutionsSummary.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsWorkflowExecutionsSummaryRequest$inboundSchema,
};

export const tool$insightsWorkflowsExecutionsSummary: ToolDefinition<
  typeof args
> = {
  name: "insights-workflows-executions-summary",
  description: `Insight Workflow Executions Summary

Return how many of a workflow's executions are in each status, optionally
within a created_at range (for example, since the workflow's \`updated_at\`
passed as \`start_datetime\`).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsWorkflowsExecutionsSummary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
