/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTargetsUpdate } from "../../funcs/channelsTargetsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChannelTargetsUpdateRequest$inboundSchema,
};

export const tool$channelsTargetsUpdate: ToolDefinition<typeof args> = {
  name: "channels-targets-update",
  description: `Edit Channel Target

Update channel target by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsTargetsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
