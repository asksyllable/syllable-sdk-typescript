/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsToolHistory } from "../../funcs/toolsToolHistory.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolHistoryRequest$inboundSchema,
};

export const tool$toolsToolHistory: ToolDefinition<typeof args> = {
  name: "tools-tool-history",
  description: `Tool History

Get version history for a tool.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsToolHistory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
