/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsPostCardClickEvent } from "../../funcs/agentsPostCardClickEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostCardClickEventRequest$inboundSchema,
};

export const tool$agentsPostCardClickEvent: ToolDefinition<typeof args> = {
  name: "agents-post-card-click-event",
  description: `Create Card Click Event

Log an EventLog for a client-side card click interaction.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsPostCardClickEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
