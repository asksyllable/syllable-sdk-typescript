/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTwilioNumbersAdd } from "../../funcs/channelsTwilioNumbersAdd.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChannelsTwilioNumbersAddRequest$inboundSchema,
};

export const tool$channelsTwilioNumbersAdd: ToolDefinition<typeof args> = {
  name: "channels-twilio-numbers-add",
  description: `Add Twilio Number

Purchase a Twilio number and associate it with a channel.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsTwilioNumbersAdd(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
