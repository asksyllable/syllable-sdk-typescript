/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsFoldersCreate } from "../../funcs/insightsFoldersCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.InsightsFolderInput$inboundSchema,
};

export const tool$insightsFoldersCreate: ToolDefinition<typeof args> = {
  name: "insights-folders-create",
  description: `Create Insights Upload Folder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsFoldersCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
