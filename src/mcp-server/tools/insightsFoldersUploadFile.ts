/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsFoldersUploadFile } from "../../funcs/insightsFoldersUploadFile.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsFolderUploadFileRequest$inboundSchema,
};

export const tool$insightsFoldersUploadFile: ToolDefinition<typeof args> = {
  name: "insights-folders-upload-file",
  description: `Upload Insights  Upload Folder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsFoldersUploadFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
