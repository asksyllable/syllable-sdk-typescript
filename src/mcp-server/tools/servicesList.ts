/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { servicesList } from "../../funcs/servicesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ServiceListRequest$inboundSchema,
};

export const tool$servicesList: ToolDefinition<typeof args> = {
  name: "services-list",
  description: `Service List

List the existing services`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await servicesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
