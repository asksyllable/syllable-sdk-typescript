/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundCampaignsGetById } from "../../funcs/outboundCampaignsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundCampaignGetByIdRequest$inboundSchema,
};

export const tool$outboundCampaignsGetById: ToolDefinition<typeof args> = {
  name: "outbound-campaigns-get-by-id",
  description: `Get Outbound Communication Campaign`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundCampaignsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
