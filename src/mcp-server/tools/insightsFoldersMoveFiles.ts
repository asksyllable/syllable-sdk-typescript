/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsFoldersMoveFiles } from "../../funcs/insightsFoldersMoveFiles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsUploadMoveFilesRequest$inboundSchema,
};

export const tool$insightsFoldersMoveFiles: ToolDefinition<typeof args> = {
  name: "insights-folders-move-files",
  description: `Move Insights Upload Files`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsFoldersMoveFiles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
