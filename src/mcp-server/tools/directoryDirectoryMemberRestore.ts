/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { directoryDirectoryMemberRestore } from "../../funcs/directoryDirectoryMemberRestore.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DirectoryMemberRestoreRequest$inboundSchema,
};

export const tool$directoryDirectoryMemberRestore: ToolDefinition<typeof args> =
  {
    name: "directory-directory-member-restore",
    description: `Restore Directory Member

Restore a soft-deleted directory member.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await directoryDirectoryMemberRestore(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
