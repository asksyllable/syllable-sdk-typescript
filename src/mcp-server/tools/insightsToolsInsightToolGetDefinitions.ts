/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsToolsInsightToolGetDefinitions } from "../../funcs/insightsToolsInsightToolGetDefinitions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$insightsToolsInsightToolGetDefinitions: ToolDefinition = {
  name: "insights-tools-insight-tool-get-definitions",
  description: `Get Insight Tool Definitions

Get a InsightTool by Name.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await insightsToolsInsightToolGetDefinitions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
