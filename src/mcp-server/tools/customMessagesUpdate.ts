/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customMessagesUpdate } from "../../funcs/customMessagesUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CustomMessageUpdateRequest$inboundSchema,
};

export const tool$customMessagesUpdate: ToolDefinition<typeof args> = {
  name: "custom-messages-update",
  description: `Update Custom Message

Update a custom message`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customMessagesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
