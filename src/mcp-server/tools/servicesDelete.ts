/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { servicesDelete } from "../../funcs/servicesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ServiceDeleteRequest$inboundSchema,
};

export const tool$servicesDelete: ToolDefinition<typeof args> = {
  name: "services-delete",
  description: `Delete Service

Delete a service. A service with linked tools cannot be deleted.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await servicesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
