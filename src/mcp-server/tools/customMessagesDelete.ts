/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customMessagesDelete } from "../../funcs/customMessagesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CustomMessagesDeleteRequest$inboundSchema,
};

export const tool$customMessagesDelete: ToolDefinition<typeof args> = {
  name: "custom-messages-delete",
  description: `Delete Custom Message

Delete custom message by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customMessagesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
