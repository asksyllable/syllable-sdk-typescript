/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { servicesUpdate } from "../../funcs/servicesUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.ServiceUpdateRequest$inboundSchema,
};

export const tool$servicesUpdate: ToolDefinition<typeof args> = {
  name: "services-update",
  description: `Update Service

Update an existing service.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await servicesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
