/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsToolsGetById } from "../../funcs/insightsToolsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightToolGetByIdRequest$inboundSchema,
};

export const tool$insightsToolsGetById: ToolDefinition<typeof args> = {
  name: "insights-tools-get-by-id",
  description: `Get Insight Tool Config By Id

Get a InsightTool by Name.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsToolsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
