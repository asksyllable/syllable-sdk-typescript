/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTwilioNumbersUpdate } from "../../funcs/channelsTwilioNumbersUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChannelsTwilioNumbersUpdateRequest$inboundSchema,
};

export const tool$channelsTwilioNumbersUpdate: ToolDefinition<typeof args> = {
  name: "channels-twilio-numbers-update",
  description: `Update Twilio Number

Update a Twilio number and associate it with a channel.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsTwilioNumbersUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
