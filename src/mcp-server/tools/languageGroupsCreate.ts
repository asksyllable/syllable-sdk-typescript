/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { languageGroupsCreate } from "../../funcs/languageGroupsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.LanguageGroupCreateRequest$inboundSchema,
};

export const tool$languageGroupsCreate: ToolDefinition<typeof args> = {
  name: "language-groups-create",
  description: `Create Language Group

Deprecated alias for \`POST /api/v1/voice_groups/\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageGroupsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
