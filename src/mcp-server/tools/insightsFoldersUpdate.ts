/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsFoldersUpdate } from "../../funcs/insightsFoldersUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsFolderUpdateRequest$inboundSchema,
};

export const tool$insightsFoldersUpdate: ToolDefinition<typeof args> = {
  name: "insights-folders-update",
  description: `Update Insights Folder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsFoldersUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
