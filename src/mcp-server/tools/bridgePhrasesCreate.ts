/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bridgePhrasesCreate } from "../../funcs/bridgePhrasesCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.BridgePhrasesCreateRequest$inboundSchema,
};

export const tool$bridgePhrasesCreate: ToolDefinition<typeof args> = {
  name: "bridge-phrases-create",
  description: `Create Bridge Phrases

Create a new bridge phrases config.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bridgePhrasesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
