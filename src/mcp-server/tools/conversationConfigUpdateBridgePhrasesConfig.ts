/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationConfigUpdateBridgePhrasesConfig } from "../../funcs/conversationConfigUpdateBridgePhrasesConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateBridgePhrasesConfigRequest$inboundSchema,
};

export const tool$conversationConfigUpdateBridgePhrasesConfig: ToolDefinition<
  typeof args
> = {
  name: "conversation-config-update-bridge-phrases-config",
  description: `Update Bridge Phrases Config

Update the bridge phrases configuration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationConfigUpdateBridgePhrasesConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
