/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsOrganizationsGet } from "../../funcs/organizationsOrganizationsGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsOrganizationsGet: ToolDefinition = {
  name: "organizations-organizations-get",
  description: `Get Current Organization

Fetch the current organization.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await organizationsOrganizationsGet(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
