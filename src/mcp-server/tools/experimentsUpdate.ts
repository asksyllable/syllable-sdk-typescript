/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsUpdate } from "../../funcs/experimentsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExperimentsUpdateRequest$inboundSchema,
};

export const tool$experimentsUpdate: ToolDefinition<typeof args> = {
  name: "experiments-update",
  description: `Update Experiment

Update an experiment. Only an experiment in 'draft' status can be updated.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
