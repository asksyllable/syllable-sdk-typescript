/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customMessagesCreate } from "../../funcs/customMessagesCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CustomMessageCreateRequest$inboundSchema,
};

export const tool$customMessagesCreate: ToolDefinition<typeof args> = {
  name: "custom-messages-create",
  description: `Create Custom Message

Create a new custom message`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customMessagesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
