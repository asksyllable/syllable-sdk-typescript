/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { languageGroupsList } from "../../funcs/languageGroupsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.LanguageGroupsListRequest$inboundSchema,
};

export const tool$languageGroupsList: ToolDefinition<typeof args> = {
  name: "language-groups-list",
  description: `List Language Groups

Deprecated alias for \`GET /api/v1/voice_groups/\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageGroupsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
