/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersUsersDeleteAccount } from "../../funcs/usersUsersDeleteAccount.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$usersUsersDeleteAccount: ToolDefinition = {
  name: "users-users-delete-account",
  description: `Request Removal Of This Account

Request removal of the account of the user calling this endpoint. Intended for removing trial
accounts.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await usersUsersDeleteAccount(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
