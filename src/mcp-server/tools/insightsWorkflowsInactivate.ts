/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsWorkflowsInactivate } from "../../funcs/insightsWorkflowsInactivate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsWorkflowInactivateRequest$inboundSchema,
};

export const tool$insightsWorkflowsInactivate: ToolDefinition<typeof args> = {
  name: "insights-workflows-inactivate",
  description: `Inactivate Insights Workflow

Inactivate an InsightWorkflow.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsWorkflowsInactivate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
