/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsList } from "../../funcs/experimentsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExperimentsListRequest$inboundSchema,
};

export const tool$experimentsList: ToolDefinition<typeof args> = {
  name: "experiments-list",
  description: `Experiments List

List the existing experiments`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
