/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionLabelsList } from "../../funcs/sessionLabelsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SessionLabelsListRequest$inboundSchema,
};

export const tool$sessionLabelsList: ToolDefinition<typeof args> = {
  name: "session-labels-list",
  description: `Session Labels List`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionLabelsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
