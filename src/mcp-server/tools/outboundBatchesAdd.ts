/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundBatchesAdd } from "../../funcs/outboundBatchesAdd.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundBatchAddRequest$inboundSchema,
};

export const tool$outboundBatchesAdd: ToolDefinition<typeof args> = {
  name: "outbound-batches-add",
  description: `Create Outbound Communication Request`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundBatchesAdd(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
