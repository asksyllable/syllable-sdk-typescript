/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsToolsDelete } from "../../funcs/insightsToolsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsToolDeleteRequest$inboundSchema,
};

export const tool$insightsToolsDelete: ToolDefinition<typeof args> = {
  name: "insights-tools-delete",
  description: `Delete Insight Tool Configuration

Delete an Insights tool configuration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsToolsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
