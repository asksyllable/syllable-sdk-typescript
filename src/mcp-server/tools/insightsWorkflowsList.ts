/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsWorkflowsList } from "../../funcs/insightsWorkflowsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsWorkflowListRequest$inboundSchema,
};

export const tool$insightsWorkflowsList: ToolDefinition<typeof args> = {
  name: "insights-workflows-list",
  description: `Insight Workflow List

List the existing insight_workflows`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsWorkflowsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
