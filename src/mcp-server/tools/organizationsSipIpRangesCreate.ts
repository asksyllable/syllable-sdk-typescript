/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsSipIpRangesCreate } from "../../funcs/organizationsSipIpRangesCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.OrganizationSipIpRangeCreate$inboundSchema,
};

export const tool$organizationsSipIpRangesCreate: ToolDefinition<typeof args> =
  {
    name: "organizations-sip-ip-ranges-create",
    description: `Create Organization Sip Ip Range

Create an unverified SIP signaling/media IP range for the current organization.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await organizationsSipIpRangesCreate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
