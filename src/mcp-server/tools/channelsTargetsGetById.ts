/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTargetsGetById } from "../../funcs/channelsTargetsGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChannelTargetsGetByIdRequest$inboundSchema,
};

export const tool$channelsTargetsGetById: ToolDefinition<typeof args> = {
  name: "channels-targets-get-by-id",
  description: `Get A Channel Target`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsTargetsGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
