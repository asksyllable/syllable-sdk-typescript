/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsWorkflowsQueueWork } from "../../funcs/insightsWorkflowsQueueWork.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.InsightsWorkflowQueueSession$inboundSchema,
};

export const tool$insightsWorkflowsQueueWork: ToolDefinition<typeof args> = {
  name: "insights-workflows-queue-work",
  description: `Queue Insights Workflow For Sessions/Files

Manually queue sessions for insights workflow evaluation.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsWorkflowsQueueWork(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
