/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsIncidentGetOrganizations } from "../../funcs/incidentsIncidentGetOrganizations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$incidentsIncidentGetOrganizations: ToolDefinition = {
  name: "incidents-incident-get-organizations",
  description: `Get Organizations

Get all organizations`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await incidentsIncidentGetOrganizations(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
