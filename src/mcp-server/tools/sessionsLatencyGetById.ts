/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsLatencyGetById } from "../../funcs/sessionsLatencyGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SessionLatencyGetByIdRequest$inboundSchema,
};

export const tool$sessionsLatencyGetById: ToolDefinition<typeof args> = {
  name: "sessions-latency-get-by-id",
  description: `Inspect Latency For Session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsLatencyGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
