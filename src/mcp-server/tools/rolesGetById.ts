/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rolesGetById } from "../../funcs/rolesGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RolesGetByIdRequest$inboundSchema,
};

export const tool$rolesGetById: ToolDefinition<typeof args> = {
  name: "roles-get-by-id",
  description: `Get Role

Fetch a given role.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await rolesGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
