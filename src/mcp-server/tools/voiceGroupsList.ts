/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { voiceGroupsList } from "../../funcs/voiceGroupsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.VoiceGroupsListRequest$inboundSchema,
};

export const tool$voiceGroupsList: ToolDefinition<typeof args> = {
  name: "voice-groups-list",
  description: `List Voice Groups

Fetch voice groups.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await voiceGroupsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
