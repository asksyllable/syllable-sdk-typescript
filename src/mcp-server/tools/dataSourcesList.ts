/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataSourcesList } from "../../funcs/dataSourcesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DataSourcesListRequest$inboundSchema,
};

export const tool$dataSourcesList: ToolDefinition<typeof args> = {
  name: "data-sources-list",
  description: `List Data Sources

Fetch metadata about all data sources, not including their text.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataSourcesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
