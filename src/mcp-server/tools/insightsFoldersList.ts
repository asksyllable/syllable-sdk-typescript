/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsFoldersList } from "../../funcs/insightsFoldersList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsFolderListRequest$inboundSchema,
};

export const tool$insightsFoldersList: ToolDefinition<typeof args> = {
  name: "insights-folders-list",
  description: `List Insights Upload Folders`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsFoldersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
