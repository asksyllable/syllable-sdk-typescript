/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dashboardsPostSessionTransfersDashboard } from "../../funcs/dashboardsPostSessionTransfersDashboard.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dashboardsPostSessionTransfersDashboard: ToolDefinition = {
  name: "dashboards-post-session-transfers-dashboard",
  description: `Post Session Transfers

METHOD: POST
URL: /dashboard/session_transfers
ARGUMENTS: None
RETURNS: Dashboard info for embedding
DEPRECATED: This endpoint is deprecated. Please use /dashboard/list instead`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await dashboardsPostSessionTransfersDashboard(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
