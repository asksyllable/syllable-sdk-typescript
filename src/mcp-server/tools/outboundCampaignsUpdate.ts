/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundCampaignsUpdate } from "../../funcs/outboundCampaignsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundCampaignUpdateRequest$inboundSchema,
};

export const tool$outboundCampaignsUpdate: ToolDefinition<typeof args> = {
  name: "outbound-campaigns-update",
  description: `Update Outbound Communication Campaign`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundCampaignsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
