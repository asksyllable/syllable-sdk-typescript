/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsSipIpRangesList } from "../../funcs/organizationsSipIpRangesList.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsSipIpRangesList: ToolDefinition = {
  name: "organizations-sip-ip-ranges-list",
  description: `List Organization Sip Ip Ranges

List SIP signaling/media IP ranges for the current organization.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await organizationsSipIpRangesList(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
