/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTwilioUpdate } from "../../funcs/channelsTwilioUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.TwilioChannelUpdateRequest$inboundSchema,
};

export const tool$channelsTwilioUpdate: ToolDefinition<typeof args> = {
  name: "channels-twilio-update",
  description: `Update Twilio Channel`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsTwilioUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
