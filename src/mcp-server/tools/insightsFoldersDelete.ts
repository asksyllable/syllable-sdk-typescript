/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsFoldersDelete } from "../../funcs/insightsFoldersDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsFolderDeleteRequest$inboundSchema,
};

export const tool$insightsFoldersDelete: ToolDefinition<typeof args> = {
  name: "insights-folders-delete",
  description: `Delete Insights Folder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsFoldersDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
