/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { voiceGroupsCreate } from "../../funcs/voiceGroupsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.VoiceGroupCreateRequest$inboundSchema,
};

export const tool$voiceGroupsCreate: ToolDefinition<typeof args> = {
  name: "voice-groups-create",
  description: `Create Voice Group

Create a new voice group.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await voiceGroupsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
