/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsSipIpRangesUpdate } from "../../funcs/organizationsSipIpRangesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OrganizationSipIpRangesUpdateRequest$inboundSchema,
};

export const tool$organizationsSipIpRangesUpdate: ToolDefinition<typeof args> =
  {
    name: "organizations-sip-ip-ranges-update",
    description: `Update Organization Sip Ip Range

Update a SIP IP range. The verified status is managed by Syllable only.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await organizationsSipIpRangesUpdate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
