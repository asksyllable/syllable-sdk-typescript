/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsWorkflowsDelete } from "../../funcs/insightsWorkflowsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InsightsWorkflowDeleteRequest$inboundSchema,
};

export const tool$insightsWorkflowsDelete: ToolDefinition<typeof args> = {
  name: "insights-workflows-delete",
  description: `Delete Insights Workflow

Delete an Insights workflow.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsWorkflowsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
