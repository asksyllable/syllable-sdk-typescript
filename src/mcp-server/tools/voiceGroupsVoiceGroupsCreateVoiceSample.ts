/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { voiceGroupsVoiceGroupsCreateVoiceSample } from "../../funcs/voiceGroupsVoiceGroupsCreateVoiceSample.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.VoiceSampleCreateRequest$inboundSchema,
};

export const tool$voiceGroupsVoiceGroupsCreateVoiceSample: ToolDefinition<
  typeof args
> = {
  name: "voice-groups-voice-groups-create-voice-sample",
  description: `Create Voice Sample

Generate voice sample.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await voiceGroupsVoiceGroupsCreateVoiceSample(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
