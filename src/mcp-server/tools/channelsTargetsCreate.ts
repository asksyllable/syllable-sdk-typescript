/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTargetsCreate } from "../../funcs/channelsTargetsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ChannelTargetsCreateRequest$inboundSchema,
};

export const tool$channelsTargetsCreate: ToolDefinition<typeof args> = {
  name: "channels-targets-create",
  description: `Assign A Channel Target`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsTargetsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
