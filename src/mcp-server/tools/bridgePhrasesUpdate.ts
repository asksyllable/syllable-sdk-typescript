/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bridgePhrasesUpdate } from "../../funcs/bridgePhrasesUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.BridgePhrasesUpdateRequest$inboundSchema,
};

export const tool$bridgePhrasesUpdate: ToolDefinition<typeof args> = {
  name: "bridge-phrases-update",
  description: `Update Bridge Phrases

Update an existing bridge phrases config.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bridgePhrasesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
