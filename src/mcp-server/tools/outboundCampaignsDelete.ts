/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { outboundCampaignsDelete } from "../../funcs/outboundCampaignsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OutboundCampaignDeleteRequest$inboundSchema,
};

export const tool$outboundCampaignsDelete: ToolDefinition<typeof args> = {
  name: "outbound-campaigns-delete",
  description: `Delete Outbound Communication Campaign`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await outboundCampaignsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
