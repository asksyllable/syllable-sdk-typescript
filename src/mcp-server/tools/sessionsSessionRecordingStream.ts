/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsSessionRecordingStream } from "../../funcs/sessionsSessionRecordingStream.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SessionRecordingStreamRequest$inboundSchema,
};

export const tool$sessionsSessionRecordingStream: ToolDefinition<typeof args> =
  {
    name: "sessions-session-recording-stream",
    description: `Stream Recording`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await sessionsSessionRecordingStream(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
